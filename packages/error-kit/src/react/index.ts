/**
 * @mapps/error-kit/react — the React surface (ErrorBoundary only). `react` is a
 * peerDependency; a non-React consumer never imports this entry.
 */
export { ErrorBoundary } from './ErrorBoundary';
