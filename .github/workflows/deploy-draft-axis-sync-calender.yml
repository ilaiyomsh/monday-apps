name: Deploy Draft - axis-sync-calender

# Spec section 7.1: merges into develop auto-deploy to the app's LATEST
# DRAFT version. No version IDs are stored anywhere — only the App ID
# secret; the CLI resolves the latest draft itself.
#
# When instantiating this template for a real app, rename the file to:
#   deploy-draft-axis-sync-calender.yml
# and replace all axis-sync-calender / . / APP_AXIS_SYNC_CALENDER_ID placeholders.

on:
  workflow_dispatch: {}   # manual re-deploy of the draft (e.g. dev-live detach); draft only — live stays merge-triggered
  push:
    branches:
      - develop
    paths:
      - "apps/axis/sync-calender/**"
      - "packages/shared/**"
      - "apps/axis/services/**"

jobs:
  deploy-draft:
    name: Build and push axis-sync-calender to draft
    runs-on: ubuntu-latest
    concurrency:
      group: deploy-draft-axis-sync-calender
      cancel-in-progress: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build axis-sync-calender
        # Version layer: stamp the exact commit into the bundle (__BUILD_SHA__).
        # Axiom error sink: bake the shared error dataset + ingest token + app slug into the
        # client bundle (error-guard remote-monitoring standard). The sink self-gates on
        # import.meta.env.PROD, so it stays inert in dev/tunnel/test builds.
        env:
          VITE_BUILD_SHA: ${{ github.sha }}
          VITE_AXIOM_DATASET: app-errors
          VITE_AXIOM_TOKEN: ${{ secrets.AXIOM_INGEST_TOKEN }}
          VITE_AXIOM_APP: sync-calender-admin
        run: pnpm --filter "./apps/axis/sync-calender" build

      - name: Install monday.com apps CLI
        run: npm install -g @mondaycom/apps-cli

      - name: Authenticate mapps CLI (non-interactive)
        run: mapps init -t "$MONDAY_TOKEN"
        env:
          MONDAY_TOKEN: ${{ secrets.MONDAY_TOKEN }}

      #  is "-c" for client-side (CDN) apps, empty for
      # server-side (monday-code) apps — the only difference between the two.
      - name: Push to latest DRAFT version
        run: >
          mapps code:push 
          -d apps/axis/sync-calender/.
          -a "$APP_ID"
        env:
          MONDAY_TOKEN: ${{ secrets.MONDAY_TOKEN }}
          APP_ID: ${{ secrets.APP_AXIS_SYNC_CALENDER_ID }}
