# This app was born error-guard-compliant.
# It ships the standard error-catching infrastructure from the start:
#   src/utils/logger.js            ג€” single logging choke-point (emit + sinks + ring buffer)
#   src/utils/globalErrorHandler.js ג€” window.onerror + unhandledrejection + resource errors
#   src/components/ErrorBoundary/AppErrorBoundary.jsx ג€” render-phase catch + Hebrew fallback
#   src/hooks/useUiErrorSink.js    ג€” one logged ERROR = one toast (wire your showToast)
#   package.json eslintConfig      ג€” no-console, no-empty, catch-must-log, promise/catch-or-return
#
# The presence of this marker enables the error-guard full-tree ship gate
# (ship.sh runs the rule kit over the ENTIRE tree, not just changed files).
# Standard: .claude/skills/error-guard (in the shared skills repo)
standard: error-guard
generated-by: monday-scaffold

