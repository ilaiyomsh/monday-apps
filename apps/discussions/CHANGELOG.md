# Changelog - discussions

*Auto-generated. Source: `~/.change-tracker/changes.db`*

## 2.1.2 — 2026-07-16

- round117: כשתיבת העדכונים פתוחה, שורת הפילטר/מיון/קיבוץ לא נעלמת מהפריסה אלא
  רק מוסתרת ויזואלית (`visibility: hidden` במקום `display: none`): הכפתורים
  אינם נראים אבל השורה ממשיכה לתפוס את מקומה, כך שהמסך לא "קופץ" למעלה. הוחל
  על כל ששת הטולברים (משימות, הנחיות קודמות, נושאים, החלטות, המשימות שלי,
  ההחלטות שלי).

- round117: ריווח אנכי סטנדרטי בתצוגות "המשימות שלי" ו"ההחלטות שלי", תואם את
  מקצב תצוגת הדיון: כותרת התצוגה → 24px → שורת הפונקציות → 16px → כותרת
  הקבוצה → 18px → הטבלה (במקום 8/16/12 הצפופים).

- round117: פס הגלילה של רשימת הדיונים מופיע רק כשהעכבר בתוך הרשימה (המקום
  שלו שמור תמיד — אין קפיצת פריסה), בלי חיצי המשולש בקצוות.

## 2.1.1 — 2026-07-14

- round116: שני ליטושי כותרת: (1) כשתיבת העדכונים פתוחה — גם פס הגלילה האופקי של
  **שורת הטאבים** מוסתר לגמרי (round113 כיסה את הטבלאות וגוף הכרטיס; זה הפס
  שנשאר). (2) קו ההפרדה שמתחת לטאבים נעצר בדיוק על הקו השמאלי של הטבלה (52px)
  ולא זולג שמאלה ממנו.

- round115: **יוצר + תאריך יצירה אוטומטיים בכל הלוחות.** בכל יצירה דרך האפליקציה
  נכתבים המשתמש היוצר (עמודת PEOPLE "יוצר") ותאריך היום (עמודת "תאריך יצירה"):
  דיונים (כולל הפעלת כתיבת היוצר שהושבתה בעבר — נכתבת רק כשהעמודה הממופה היא
  PEOPLE רגילה, לא Creation-log), משימות (טאב משימות + המשימות שלי), נושאים
  ונקודות (כולל יצירה מתבניות — שם נוסף גם היוצר). בהחלטות היוצר והתאריך כבר
  נכתבו קודם. שלושה אליאסים חדשים בסכמה + שדות מיפוי בהגדרות + יצירה אוטומטית
  באשף ההקמה: `taskCreationDateID`, `topicCreationDateID`, `pointCreationDateID`
  (כולם בשם "תאריך יצירה"; בחשבון הזה העמודות כבר קיימות — נותר רק למפות).

- round114: יישור הגבול השמאלי בכל האפליקציה + ליטושים: (1) "המשימות שלי"
  ו"ההחלטות שלי" קיבלו את אותו מרווח לבן חלק (52px) משמאל — החץ לחזרה, כפתור
  "משימה חדשה"/"החלטה חדשה" וכל רכיב אחר מיושרים בדיוק על קו הקצה השמאלי של
  הטבלה ולא חורגים ממנו. (2) גם טאב אפקטיביות מיושר לאותו קו (היה 16px).
  (3) הטאב "הנחיות קודמות" יושב בדיוק על הקו, מיושר עם קצה הטבלה שמתחתיו.
  (4) בורר אנשים נסגר מיידית כשמסירים את האדם האחרון והעמודה מתרוקנת.
  (5) אייקון הלינק הוסר מכותרת הדיון ועבר לתפריט השלוש-נקודות של שורת הדיון —
  בסדר: עריכה → לינק לדיון (מציג ✓ הועתק) → שכפול → ייצוא → מחיקה; זמין גם
  בתפריט קליק-ימני וגם ביומן. (6) כפתור ה-+ הכחול בטאבי סיכום/אפקטיביות הוקטן
  ב-30% (56px → 39px).

- round113: כשתיבת העדכונים / כרטיס הפריט של monday פתוחים (מצב chrome-narrow של
  round103) — **פסי הגלילה מוסתרים לגמרי** בכל אזורי הגלילה של האפליקציה (גוף
  הכרטיס, טבלאות משימות/קודמות/נושאים/החלטות/אפקטיביות, המשימות שלי וההחלטות
  שלי). הגלילה עצמה ממשיכה לעבוד (גלגלת/מגע) — רק הפסים לא נראים.

- round112: חמישה ליטושים שאושרו בהמחשה: (1) חץ פרטי הדיון פונה **שמאלה** ופתיחתו
  מציגה את מנהל/מרכז/משתתפים **בשורה משמאל כמו קודם** (בוטלה השורה השנייה של
  round111; החץ מתהפך לסגירה). (2) צד ימין של הטבלאות (משימות/קודמות/המשימות שלי)
  במילוי תאים ריקים כמו מאנדיי — קווי השורות נמשכים עד קצה המסך, בלי ה-+.
  (3) מרווח לבן חלק של 52px משמאל לכותרת ולכל הטאבים (טוקן `--content-gutter-start`),
  והמשטח הלבן קיבל פינה שמאלית-עליונה מעוגלת (8px) מול רקע אפרפר. (4) ידית הרחבת
  רשימת הדיונים המכווצת מוסתרת לגמרי ומופיעה רק בקרבת העכבר לקצה. (5) עמודות
  PEOPLE ריקות בכל האפליקציה מציגות את הגליף המדויק של מאנדיי — טבעת אפורה עם
  ראש משיק לכתפיים שנחתכות בקשת (`EmptyPersonGlyph` משותף ל-PersonPicker ו-PersonList).

- round111: פרטי הדיון בכותרת נפתחים/נסגרים — כברירת מחדל רואים רק תאריך ושעה
  (עם מרווח מוגדל ביניהם) וכפתור חץ עגול משמאלם; לחיצה פותחת מתחת שורה שנייה
  באותו רוחב עם בעלי התפקידים והמשתתפים, כל תפקיד ככותרת ומתחתיה האווטאר.
  העריכה (תאריך/שעה/אנשים) נשמרה למורשים, וההסתרה בעת פתיחת תיבת העדכונים
  (round103) חלה כמו קודם. בנוסף: בחירת דיון מכווצת אוטומטית את רשימת הדיונים
  הצידה באנימציה של ~0.3 שניות (בדסקטופ; ידית הקצה מחזירה אותה), וגם כפתור
  הכיווץ הידני מונפש עכשיו.

- round110: צ'יפ עמודת Connected תופס תמיד את מלוא רוחב התא — רוחב אחיד וקבוע
  לכל הצ'יפים, בלי תלות באורך הטקסט (בטאב משימות וב"משימות שלי"; "ההחלטות שלי"
  כבר התנהג כך). הטקסט נשאר ממורכז והפס בקצה השמאלי.

- round109: בעורך תבניות הנושאים/נקודות — הנקודות מתחילות עמוק יותר ימינה מתחת
  לנושא (ההזחה הועמקה מ-24px ל-44px), כך שההיררכיה נושא→נקודות ברורה יותר.

- round107: צ'יפ עמודת Connected שוחזר פיקסל-פיקסל לפי צילום המסך ממאנדיי (נמדד
  בקוד): פס ההדגשה עכשיו **קצר** (14px), ממורכז אנכית, בכחול בהיר `#66abf2` (במקום
  פס כמעט-מלא-גובה בכחול רווי), הטקסט אפור `#676879` בגודל 13px **וממורכז** בתוך
  הצ'יפ, גובה אחיד 32px. חל על משימות, המשימות שלי וההחלטות שלי; פס הסגול הייחודי
  של ההחלטות הוסר לטובת נאמנות מלאה למאנדיי.

- round108: ה-OWNER יכול להעלות **לוגו** שמוצג בפינה הימנית-העליונה של כותרת הדיון
  (בגובה שם הדיון, בצד הימני של המסך). ההעלאה נמצאת בהגדרות → העדפות: בוחרים קובץ
  תמונה, הוא מוקטן אוטומטית ל-data-URI קטן (ללא אחסון קבצים חיצוני) ונשמר תחת
  `preferences.logoUrl`; יש תצוגה מקדימה וכפתור "הסר". הלוגו מוצג לכל המשתמשים בתצוגה,
  אך רק ה-OWNER יכול להגדירו (מסך ההגדרות ממילא owner-only). מוסתר בפריסת המובייל.

- round103: כשתיבת העדכונים (item card) של monday נפתחת היא מצמצמת את רוחב ה-iframe,
  ובעבר זה "גרר" את הרכיבים שמאלה. עכשיו גלאי-רוחב יחסי (`chromeNarrow`) מזהה את
  הצמצום ומסמן `body[data-chrome-narrow]`, וכל תצוגה **מסתירה** את הצ'רום הלא-חיוני
  בזמן שהפאנל פתוח — פרטי הדיון בכותרת (תאריך/שעה/מנהל/מרכז/משתתפים), סוללת הסינון
  המהיר, וכל שורת החיפוש/סינון/מיון — ומחזירה אותם כשהפאנל נסגר. חל בכל התצוגות
  (משימות/קודמות/נושאים/החלטות/המשימות שלי/ההחלטות שלי). הסף יחסי (80% מהרוחב המרבי)
  כך שצמצום קל של החלון אינו מפעיל אותו.

- round104: תיקון שגיאת GraphQL בהקצאת אדם לעמודת people בהחלטות ("מושפעים" /
  "מחליט"). מאז round79 בורר "מושפעים" מציע את כל חשבון monday, ו-monday דוחה
  הקצאת אדם שאינו מנוי (subscriber) על לוח ההחלטות. עכשיו — בדיוק כמו monday —
  לפני הכתיבה מצרפים את הנבחר כמנוי ללוח (`ensureSubscribers`, best-effort). חל על
  עריכת מושפעים/מחליט ב"ההחלטות שלי" ובטאב ההחלטות, וגם על יצירת החלטה חדשה.

- round102: צ'יפ העמודה מסוג Connected (דיון מקור/מקושר) עוצב בדיוק כמו monday —
  מסגרת בהירה אחידה מסביב, פס-הדגשה **מעוגל ומוזח** בקצה השמאלי (במקום גבול שמאלי
  שטוח של 3px), רקע לבן, טקסט אפור כהה ב-14px וגובה נוח. חל על משימות, המשימות שלי
  וההחלטות שלי (בהחלטות הפס בסגול הדגשת ההחלטות).
- round101: רשימת הלייבלים בבורר הסטטוס/עדיפות הורחבה מעט — הריפוד בצדדים גדל
  ל-10px ורוחב המכל גדל ב-20px, כך שרוחב **הלייבלים עצמם** שווה לרוחב העמודה
  ומשני צדיהם יש שוליים לבנים (במקום שהלייבל יהיה צר מהעמודה). חל על משימות,
  המשימות שלי, ההחלטות שלי וטאב ההחלטות.
- round100: הפס הצבעוני של הקבוצה מקבל עכשיו קצה מעוגל נקי **גם למעלה וגם למטה**
  בכל התצוגות (משימות, קודמות, נושאים, החלטות, המשימות שלי, ההחלטות שלי). ה-"צ'ופצ'יק"
  בקצה העליון בטאב נושאים נבע מרדיוס 8px על פס של 6px (box-shadow) שסלסל את הפינה
  ימינה; אוחד הרדיוס ל-6px (= רוחב הפס) כך שהקשת מוכלת בתוך הפס ומתקבל קצה מעוגל
  נקי בלי בליטה — זהה בשני הקצוות. מבטל את הקצה המרובע (round91).
- round99: עמודת "#" (נידונה) בטאב נושאים עוצבה כמו עמודת checkbox אמיתית של
  monday — התא כולו לחיץ: ריק כשלא מסומן, ולחיצה שמה **וי ירוק** ללא מסגרת (לחיצה
  נוספת מסירה אותו). על ריחוף מעל תא ריק מופיע וי אפור עמום כרמז (כמו monday).
  מחליף את ה-Checkbox המרובע מ-round96.
- round98: המשך ליטוש עמודות STATUS/עדיפות — (א) טקסט הלייבל (גם בתא וגם בבורר
  הנפתח) עבר למשקל **רגיל** (כמו שם המשימה, לא bold), בצבע לבן; (ב) רוחב הלייבלים
  בבורר הנפתח שווה עכשיו לרוחב הלייבל בעמודה (נמדד מהתא) במקום 206px קבוע. חל על
  משימות, המשימות שלי, ההחלטות שלי וטאב ההחלטות.
- round97: בבורר האנשים (PersonPicker, בכל הקצאת אדם) — (א) שורת החיפוש עברה ל-RTL
  כך שהקלדת שמות בעברית נכתבת מימין לשמאל (אייקון החיפוש עבר בהתאם לימין); (ב) הוסרה
  שורת הכותרת "אנשים מוצעים" — הרשימה מוצגת ישירות מתחת לחיפוש.
- round96: עמודת הצ'קבוקס "נידונה" בטאב נושאים הוחלפה ב-Checkbox המקורי של
  @vibe/core — אותו רכיב שעמודת checkbox רגילה ב-monday מרנדרת — במקום הכפתור
  המרובע שנכתב ידנית. עכשיו היא נראית ומתנהגת בדיוק כמו עמודת checkbox סטנדרטית
  (כולל מצב read-only מעומעם כשאין הרשאת סימון או שהנקודה מוסתרת).
- round95: הרצועה שבין שורת הטאבים העליונה לסרגל הסינון (Filter/Sort/Group) קיבלה
  רקע לבן מפורש. עד כה ל-`.body` (אזור הגלילה) לא היה רקע משלו — ה-header והסרגל
  צבעו תיבה לבנה, אבל פס ה-24px שביניהם נשען על רקע `.root` בלבד ובתוך ה-iframe של
  monday הציג את רקע הרקע/הערכה מאחוריו (נראה שקוף). עכשיו כל העמודה — מהטאבים דרך
  הרצועה, הסרגל והשורות — לבנה אחידה.
- round94: בורר עמודת ה-STATUS עוצב בדיוק כמו monday בכל רחבי האפליקציה — הפונטים
  הופחתו מ-bold (700) ל-medium (500) והוקטנו ל-14px, רוחב התפריט אוחד ל-206px,
  גובה כל לייבל ל-34px והרווח ביניהם ל-6px, ופינות התפריט/הלייבלים מעוגלות עדין
  יותר. בנוסף הבורר נפתח עכשיו **ממורכז** ביחס למרכז התא (במקום מיושר לקצה) ונפתח
  כלפי מטה כברירת מחדל (מתהפך מעלה רק כשאין מקום). חל על כל חמשת הבוררים: משימות,
  המשימות שלי, ההחלטות שלי, כרטיס המשימה, וטאב ההחלטות.
- round93: תיקון להקפאת הסרגל (round90) — שורות היו "מבצבצות" מעל הסרגל הקפוא כי
  לאזור-הגלילה יש padding-top של 24px שהסרגל ננעל מתחתיו. נוסף רקע לבן (::before)
  שמכסה את פס ה-24px מעל הסרגל, כך שתוכן נגלל לא מופיע מאחוריו/מעליו (בטאב משימות,
  קודמות והחלטות).
- round92: ה-avatar של עמודת PEOPLE ריקה הותאם בדיוק ל-monday — עיגול לבן עם
  מסגרת אפורה דקה וצללית משתמש אפורה (מראה "רפאים"/מתוחם), במקום הדיסק האפור המלא
  עם צללית לבנה מ-round88. חל בכל המקומות (PersonPicker בעריכה, PersonList
  בקריאה-בלבד) — משימות, קודמות, המשימות שלי, החלטות, ההחלטות שלי.
- round91: הוסר ה"צ'ופצ'יק" בקצה העליון של הפס הצבעוני של הקבוצה — עיגול הפינה של
  הפס (box-shadow) יצר בליטה שזלגה ימינה; הפס עכשיו בר אנכי ישר ונקי כמו monday
  (אופסו עיגולי הפינות של הפס בכל טבלאות הפס: משימות / קודמות / המשימות שלי /
  החלטות / ההחלטות שלי). הריבוע-מסגרת של הטבלה נשאר מעוגל.
- round90: סרגל הכלים (Filter/Sort/Group/Hide + סוללת הסינון) נשאר עכשיו **קפוא**
  בראש בזמן גלילה, בדיוק כמו שורת הטאבים — בטאב המשימות, המשימות הקודמות
  וההחלטות (sticky בתוך גוף הכרטיס). במסך "המשימות שלי" וב"החלטות שלי" הסרגל כבר
  היה קפוא (יושב מעל אזור הגלילה) וגובּה עם sticky ליתר ביטחון.
- round89: הפס הצבעוני של הקבוצה הוא עכשיו **רציף** כמו ב-monday. עד כה קווי-
  ההפרדה של השורות חצו את הפס וקטעו אותו לקטעים; הועברו הקווים מגבול-השורה
  לגבול פר-תא + קו מוזז ב-6px בתא הקפוא, כך שהם מתחילים אחרי הפס ולא חוצים אותו
  (בטבלת המשימות, הקודמות והמשימות שלי; כולל מצב בחירה).
- round88: תיקוני נאמנות ל-monday שבסבב הקודם כמעט לא נראו (השתמשתי במשתני-טוקן
  שערכם האמיתי כמעט לבן) — (א) הגבולות הובהרו לצבע ליטרלי #e6e9ef; (ב) ה-avatar
  הריק בעמודת people הוא עכשיו דיסק אפור מלא עם צללית לבנה (כמו monday), במקום
  עיגול כמעט-שקוף; (ג) תפריט בחירת הסטטוס נפתח כלפי מטה (כמו monday) במקום מעלה.
- round87: התאמות monday נוספות לטבלאות המשימות — (א) קווי הרשת (גבולות עמודות/
  שורות) הובהרו לאפור עדין כמו monday; (ב) תא people ריק מציג עכשיו avatar ריק
  בסגנון monday (עיגול אפור עם צללית משתמש) בכל האפליקציה, במקום טקסט "לא הוקצה";
  (ג) אייקוני סרגל הכלים (Search/Filter/Sort/Hide/Group by) צובעו אפור כמו monday.
- round86: תפריט בחירת הסטטוס (בכל עמודות ה-Status — משימות / קודמות / המשימות
  שלי / ההחלטות שלי / כרטיס משימה) עוצב מחדש לפרופורציות של monday: תפריט רחב
  יותר, אופציות גבוהות יותר עם טקסט לבן מודגש וממורכז, מרווחים ופדינג כמו בבורר
  ה-Labels של monday; מלל הסטטוס בתא הודגש (semibold).
- round85: פינת הפס הצבעוני של הקבוצות (שמאל-עליון) עוגלה בעדינות (4px במקום 8px)
  בכל טבלאות המשימות (משימות / קודמות / המשימות שלי) — הפס נשאר אנכי עם קצה מעוגל
  קטן במקום קימור גדול, בהתאמה לפינת הקבוצה של monday.
- round84: עיצוב סוללת הסינון הותאם לסוללת monday — ללא גבולות, המספרים כמלל אפור
  פשוט (בלי תיבה), ורק הצ'יפ הנבחר מקבל מילוי כחול-בהיר.
- round83: סוללת הסינון בטאב המשימות ובמשימות הקודמות הוזזה כחצי ס"מ שמאלה מקצה
  הסרגל (במסך "המשימות שלי" לא שונתה).
- round82: ליטוש סוללת הסינון המהיר — הועברה לצד ימין בכל התצוגות (הסינון/מיון/
  קיבוץ נשארים בצד שמאל), הסדר שונה ל(ימין→שמאל) פתוחות → בעיכוב → בוצעו,
  והצ'יפים עוצבו מלבניים יותר עם מצב-נלחץ נקי בסגנון סוללת monday.
- round81: בטאב המשימות, בטאב המשימות הקודמות ובמסך "המשימות שלי" נוספה בצד
  ימין-למעלה "סוללת" סינון מהיר בסגנון monday — שלושה צ'יפים עם מונה: משימות
  פתוחות (כתום), בוצעו (ירוק) ובעיכוב (אדום = דד-ליין שעבר וטרם בוצע). לחיצה על
  צ'יפ מסננת מיידית לאותה קבוצה, לחיצה נוספת מבטלת. הגדרות ה"בוצע"/"בעיכוב"
  משותפות עם טאב האפקטיביות (אותה משמעות בכל האפליקציה).
- round80: פלטת הצבעים של כותרת הקבוצה (קליק ימני) נפתחת עכשיו מעט מתחת לסמן כדי
  שלא תכסה את מלל הכותרת, וממוקמת בתוך גבולות המסך.
- round79: בורר ה"מושפעים" בהחלטות (גם בטאב ההחלטות בתוך הדיון וגם ב"ההחלטות
  שלי") מציע עכשיו את כל משתמשי החשבון, לא רק את חברי לוח ההחלטות/משתתפי הדיון
  (prop חדש accountWide ל-PersonPicker). הבורר של "מחליט" נשאר מגודר-לוח.
- round78: בהגדרות → מיפוי, מתחת לעמודות "יכולת צפייה" ו"יכולת עריכה", אפשר
  לבחור אילו תפקידים מלוח הדיונים (מנהל דיון / מרכז דיון / יוצר הדיון / משתתפים)
  ממלאים אוטומטית כל עמודה ביצירת משימה. ברירת המחדל משחזרת את ההתנהגות הקודמת
  (משתתפים→צפייה, מנהל+מרכז+יוצר→עריכה); האנשים מהתפקידים הנבחרים מאוחדים.

- round77: קליק ימני על כותרת של קבוצה (GROUP BY) פותח פלטת צבעים; הצבע שנבחר
  נשמר לאותה כותרת מעתה ואילך ומשותף לכל המשתמשים (monday.storage לפי מופע).
  זמין בכל תצוגות הקיבוץ (משימות שלי, טאב משימות, משימות קודמות, החלטות,
  ההחלטות שלי); "צבע אוטומטי" מחזיר לצבע ההאש. הצבע גובר על צבע אוטומטי/סמנטי.
- round76: במיפוי המרובה של עמודות אנשים (יכולת צפייה / יכולת עריכה) הצ'יפים
  מציגים עכשיו את שם העמודה במקום המזהה, ורשימת "הוסף עמודה נוספת" כבר לא מציעה
  עמודה שכבר נבחרה (תוקן: הקוד קרא value/label במקום id/name).
- round75 (תיקוני ביקורת, חומרה גבוהה): מנגנון supersede ב-useTopics (נושאים לא
  מוצגים תחת דיון שגוי); ניסיון-חוזר של יצירת משימה/החלטה/נושא מתחדש משלב הקישור
  ולא יוצר כפילות בלוח; שגיאות טעינת דיונים עוברות ל-logger ולא ל-console; פילטר
  "יוצר משימה" משתמש ב-person-<id>; TasksTab טוען את עמודות ההרשאות (יכולת
  צפייה/עריכה) כדי שההרשאות לא ידחו עורך לגיטימי; SettingsContext טוען מחדש כשה-
  instanceId האמיתי מגיע אחרי ה-watchdog.
- round74: הערות במשימות שלי נשמרות בלי כפתור "שמור"; בר הטעינה של יצירת דיון
  בולט יותר; תאי מחליט/מושפעים ב"ההחלטות שלי" נפתחים לעריכה (PersonPicker,
  מגודר הרשאות פר-שורה); הצ'קבוקס "המחליט כברירת מחדל הוא מנהל הדיון" בהגדרות
  → תבניות בולט ו-RTL; טופס משימה חדשה מיושר לימין; פופאפ יצירה מטאב ההחלטות
  נפתח במרכז המסך.

## 2026-07

### 🐛 Bug Fixes

- **2026-07-07** — getAccountSlug (subscribers.js:37): silent catch swallows API errors — add logger.warn in the catch + a test asserting the warn; merge branch test-guard-v2-shakedown into main first (carries the module's test file) `d459053`
  - _Why:_ Silent catch violates the error-guard standard (every catch must log/rethrow/display); found by the error-guard hook during the test-guard v2 shakedown
  - _Requested:_ תיקון בליעת שגיאה שקטה ב-getAccountSlug (subscribers.js:37) — הוספת לוג אזהרה בקאץ' + טסט; מיזוג ענף test-guard-v2-shakedown למיין; התאמת hook של error-guard לדלג על קבצים עם מוטציה חמושה
  - _Done:_ Plan (approved): merge the test-guard-v2-shakedown branch into main, then fix the silent catch in getAccountSlug via strict TDD (failing test first, red/green gates, 2 killed mutations), and adapt the error-guard hook to skip files carrying an armed test-guard mutation. Done: branch merged (90cb6f9, +468 test lines); the catch now logs the module's standard warn before memoizing null, gated end-to-end — red observed, green passed, both mutations (warn removal, memo-guard inversion) killed, all 415 app tests green (d459053); error-guard-check.sh now skips files with a .mutbak sibling, proven live mid-protocol. Deviations: one unplanned fix — test-guard's own Bash lock falsely blocked the sanctioned green command (its regex treated the fd redirect 2>&1 as a file clobber); fixed, fixture-verified both directions, and recorded as contract amendment 5. The error-guard adaptation was executed mid-mutation (earlier than planned) because the collision fired live at that exact moment.
- **2026-07-03** — Status/priority label pickers auto-close on select across all task tables `047aec4`
  - _Why:_ @vibe Dialog ORs its internal open-state with the controlled open prop, so the click showTrigger kept menus open after picking a label
  - _Done:_ Plan: make every inline status/priority label picker close automatically the moment a label is chosen. Done: traced @vibe's Dialog to isOpen = (internalState || open); since these menus open via showTrigger ['click'] the internal state stays true and the controlled setOpen(false) can never hide it, so added 'onContentClick' to the hideTrigger array of all five status/priority pickers (MyTasksRow's shared StatusEditCell, TaskTableRow status+priority, TaskCard status, TopicsTab priority) — clicking any label now fires @vibe's internal hide path. Verified empirically against the real Dialog and via the sourcemap, and added a regression test (statusMenuAutoClose) driving the real MyTasksRow, seen failing before the fix. No deviations.
- **2026-07-03** — Frozen columns (checkbox+name+color strip) detached from the board edge during horizontal scroll in My Tasks (scrolled columns peeked in the 28px gutter) and the add-task footer row scrolled away `9a95d44`
  - _Why:_ Chrome resolves position:sticky insets against the scrollport minus the scroller's own padding; moved the 28px inset from padding to margin-inline-start on the .board scroller, and made the add-row label+strip sticky in MyTasksTable + shared TaskTable
  - _Requested:_ יש לי בעיה שאף סוכן לא הצליח לפתור... העמודה השמאלית של שם המשימה והצקבוקס צריכות להיות קפואות, רק העמודות צריכות להיגלל... וגם שהשורה התחתונה נגללת, הוסף משימה צריך להישאר קפוא; גם בגלילה צריך להיות רווח בין הקצה השמאלי של הטבלה לקצה השמאלי של המסך
  - _Done:_ Plan: keep the frozen checkbox+name+color-strip columns pinned to the board edge during horizontal scroll instead of detaching. Done: pinned the frozen columns flush to the edge during h-scroll (375ccfa) and made the frozen name/checkbox cells opaque at rest to stop content bleeding through while scrolling (9a95d44). No deviations. Actual time is a retrospective estimate.

### ✨ New Features

- **2026-07-03** — My Tasks inline task creation: group add-row and blue toolbar button create a task named 'משימה חדשה' immediately (no modal) — add-row seeds the group's status/priority value, blue button appends to the bottom of the topmost group; discussion-grouping creates unlinked (lands in ללא דיון); MyTaskModal removed; creator stamping verified (already implemented) `0949817`
  - _Why:_ Faster monday-like task creation flow; the full modal is unnecessary friction
  - _Requested:_ לחיצה מהכפתור הכחול תפתח משימה בקבוצה העליונה; הוסף משימה בקבוצה יוצר מיד שורה בשם משימה חדשה עם ערך הקבוצה כשמוגדר group by; הכפתור הכחול עם group by יוצר בתחתית הקבוצה העליונה עם ערך הקבוצה; בכל יצירת משימה למלא את יוצר המשימה בעמודה הממופה
  - _Done:_ Plan: let users create tasks inline in the My Tasks tab without opening a modal. Done: added an inline group add-row and a blue toolbar create flow in MyTasksTable/MyTasksView backed by useMyTasks, with a smoke test (commit 0949817). No deviations. Actual time is a retrospective estimate.
- **2026-07-03** — Shared saved view selections (filter/sort/group) across all tables: Save button next to Clear in every builder dropdown, empty defaults (remove built-in My Tasks defaults), and a general permission checkbox controlling whether non-owner board members see the Save button `5c7d685`
  - _Why:_ Users want table view choices (filter/sort/group) to persist and be shared account-wide instead of resetting or staying per-device
  - _Requested:_ עבור filter/group by/sort אפשרות לשמור את הבחירה. ברירת המחדל ללא בחירות בכלל; כפתור save ליד clear שישמור עבור כל המשתמשים בחשבון; הרשאה כללית מי יכול לשנות בחירות שמורות; ליישם בכל הטבלאות עם כפתורים כאלה
  - _Done:_ Plan: persist per-control saved view selections (filter/sort/group) shared across all users of an instance, gated by a new saveViewDefaults permission. Done: added a Save button next to Clear on each builder panel (My Tasks filter/sort/group, TasksTab group, PreviousTasksTab filter/group) that writes the current selection to settings.preferences.savedViews as the load-time default for all users, gated by the saveViewDefaults capability (commit 1f39bd1). Deviation: follow-up 5c7d685 made the builder panels start fully empty (placeholder fields, no pre-seeded values). Actual time is a retrospective estimate.
- **2026-07-02** — Add user-adjustable column widths + column order to all discussion-view tables: Topics table gets both resize and reorder (new); the shared TaskTable (new tasks / previous tasks / effectiveness tabs) gets resize added to its existing reorder — one shared setting (tableId 'tasks') covers all 3 tabs, mirroring the My Tasks implementation. Name columns become fixed+resizable (like My Tasks). `9301b1b`
  - _Why:_ Column width/order customization already exists in the My Tasks tab; the user wants the same capability in every table in the discussion view for consistency
  - _Requested:_ is a my task tab. there the user can change the columns width and their order. i want it for all tables, for topics and tasks in discussion view. two tables: topics, and one set of settings for all 3 task tables in previous tasks, new tasks and effectiveness tabs
  - _Done:_ Plan: extend the My Tasks column resize+reorder capability to every discussion-view table. Done: added both resize and reorder to the Topics table and added resize to the shared TaskTable (one 'tasks' tableId covering the new-tasks, previous-tasks, and effectiveness tabs); name columns became fixed+resizable, mirroring My Tasks (commits 5a9e9ef, 5f2e7a6). Deviation: the TopicsTab name column needed a follow-up fix to become resizable (it was a fill track) — 9301b1b. Actual time is a retrospective estimate (closed in a later session).

### 🔧 Feature Changes

- **2026-07-03** — My Tasks tab: make deadline and task-name columns inline-editable, and gate ALL task edits there (status/priority/deadline/name) through the tasks-board permission matrix (editTaskStatus/editTaskPriority/editTaskDeadline/editTaskName), resolved per task from its people columns (creator/responsible). Includes resolveCan support for a task ctx without a discussion. `230b93e`
  - _Why:_ User request: deadline + name are locked in My Tasks even for the task creator; they should obey the task permissions matrix like every other column (reverses backlog #7 keep-read-only decision).
  - _Requested:_ אני רוצה שהיא תהיה כפופה להרשאות, כמו כל עמודה אחרת. הרשאות לפי עמודות הpeople בלוח משימות. גם שם המשימה נעול לעריכה בטאב משימות שלי. גם הוא צריך להיות כפוף להרשאות במודול הרשאות של משימות
  - _Done:_ Plan: make the My Tasks deadline and task-name columns inline-editable while obeying the board-permissions matrix per task. Done: added an inline deadline DatePickerPopover cell and a hover-pencil rename, each matrix-gated via usePermission against the task's own creator/responsible people columns (there is no parent discussion in this surface); updated tests and CLAUDE.md (commit 230b93e). Deviation: with permissions off this resolves to 'creator/responsible edit their own tasks', which reproduces the old ungated behavior since the view only shows my tasks. Actual time is a retrospective estimate.
- **2026-07-03** — Discussion header meta row: tighter spacing, vertical separators between fields, bold date/time, and click-to-edit date & time that persist to the discussion item `d6fa92f`
  - _Why:_ UX polish + inline editing of the discussion date/time from the header (user request)
  - _Requested:_ תצמצם רווחים, תשים קו אנכי מפריד בין השדות, תאריך ושעה בבולד, לחיצה על תאריך ושעה פותחת מודול לבחירת תאריך חדש או לבחירת שעה חדשה, וזה מתעדכן באייטם דיון עצמו
  - _Done:_ Plan: tighten the discussion header meta row — separators, bold date/time, and click-to-edit date & time. Done: added vertical separators and bold date/time to DiscussionCard's meta row and made the date and time click-to-edit via DatePickerPopover (commit d6fa92f). No deviations. Actual time is a retrospective estimate.
- **2026-07-02** — EffectivenessTab: dynamic per-status KPI cards (% + count on hover), deadline-based delayed card & chart bar in unique dark red + clock icon, new settings field to pick which statuses count as done for the delay logic `8deea31`
  - _Why:_ User feedback (Ido's Loom video review, backlog item #11)
  - _Requested:_ בקלוג #11: להחליף את כרטיס 'תקוע' — כרטיסי KPI דינמיים לכל סטטוס שיש בו משימות (אחוז + מספר בריחוף), כרטיס 'בעיכוב' = עבר דדליין וטרם בוצע לפי שדה הגדרות חדש שבוחר אילו סטטוסים נחשבים בוצע, עמודת בעיכוב בגרף, אדום כהה ייחודי + צלמית שעון
  - _Done:_ Plan: replace the fixed 'stuck' KPI card in EffectivenessTab with dynamic per-status cards and a deadline-based delayed metric (Ido video backlog #11). Done: per-status KPI cards showing percentage + count on hover, a 'delayed' card and chart bar in a unique dark red with a clock icon for items past deadline and not in a done status, and a new settings field to pick which statuses count as done (commit b35742e). Deviations: follow-ups fixed singular Hebrew labels and redesigned the card (197cda1, 8deea31). Actual time is a retrospective estimate.

### 🎨 Design

- **2026-07-03** — Align toolbar controls (Filter/Group by/Collapse) next to the left-side buttons in all discussion tables, matching My Tasks `52a4b90`
  - _Why:_ UI consistency across tabs (user request)
  - _Requested:_ אני רוצה את כל הכפתורים בשורה למעלה, של כיווץ, ופילטר ו-group by, שיהיו צמודים לשמאל כמו בטאב משימות שלי. פשוט מה שיש עכשיו, רק צמוד למה שכבר קיים בשמאל. עבור כל הטבלאות
  - _Done:_ Plan: left-align the table toolbar controls (Filter/Group by/Collapse) next to the primary actions so all tables match the My Tasks toolbar. Done: left-aligned the toolbar controls across TasksTab and PreviousTasksTab with the accompanying CSS changes, plus a TopicsTab CSS tweak (commit 52a4b90). No deviations. Actual time is a retrospective estimate.

## 2026-06

### 🐛 Bug Fixes

- **2026-06-28** — Fix the My Tasks frozen name column: removed overflow:hidden from .taskTable, which was capturing position:sticky and pinning the sticky cell to a box that scrolls with the row, so the left name column never stayed frozen during horizontal scroll `4aa2443`
  - _Why:_ User reported on mobile that the left task-name column was not frozen; root cause is an overflow:hidden ancestor between the scroll container (.taskTableScroll) and the sticky .taskFirst cell
  - _Requested:_ העמודה השמאלית של שם המשימה לא קפואה
  - _Done:_ Plan: fix the My Tasks frozen name column, reported as not sticking during horizontal scroll on mobile. Done: removed overflow:hidden from .taskTable — an ancestor with overflow!=visible was capturing the sticky cell's scroll context and pinning it to a box that scrolls WITH the row, so the name column never stayed frozen — and made the frozen cell opaque (solid white at rest; the hover state composites the semi-transparent monday hover token over white) so horizontally-scrolled cells no longer bleed through it. Verified build + tests; deployed via deploy:force. Also discovered and saved as a memory that deploy:force reuses the same live version + CDN URL, so the monday mobile app serves a cached bundle until a new version is created or the app is force-quit.
- **2026-06-28** — Settings + tables UI polish after the tabs refactor: fixed the column-picker dropdown opening in the wrong spot by portaling it to <body> (the Vibe Tabs transform broke position:fixed); moved the picker's clear (×) to the trailing edge; removed the 'not verified' warning so an unmapped column just shows the 'בחר עמודה' placeholder; left-aligned the picker label, placeholder and the task-name COLUMN HEADER (Tasks/Previous-tasks/My-Tasks) to match the left-aligned values; removed the showMyTasks subtitle and stray quotes; and stripped trailing ellipses ('...'/'…') from all user-facing strings (i18n + components). `fe834fe`
  - _Why:_ The Settings tabs refactor (#107) regressed the column picker (dropdown misposition, × in the middle) and surfaced inconsistent alignment and noisy '…'/'not verified' affordances.
  - _Requested:_ סבב פוליש: תיקון מיקום תפריט הבורר, X לצד, הסרת 'לא מאומת', יישור תווית/placeholder/כותרת-שם לשמאל, הסרת כותרת משנה וגרשיים, והסרת שלוש הנקודות מכל מקום.
  - _Done:_ Polish round following the #107 Settings-tabs refactor. Fixed the column-picker dropdown that opened detached (Vibe TabPanels apply a transform, which reparents position:fixed) by rendering it through a React portal to document.body and teaching the outside-click handler about the portaled node; moved the picker clear (×) to the trailing edge by letting the trigger text flex-grow; removed the '⚠ לא מאומת' indicator so an unmapped column simply shows the 'בחר עמודה' placeholder; left-aligned the picker label, the placeholder/value text, and the task-name COLUMN HEADER across the Tasks, Previous-tasks and My-Tasks tables (a higher-specificity .taskHead .nameHead rule) to match the left-aligned name values; removed the showMyTasks preference subtitle and the stray RTL-escaping quotes; and stripped trailing ellipses ('...' and '…') from every user-facing string (he/en i18n + eight components). No functional behavior beyond these UI fixes; internal non-UI ellipses (logger truncation, error parsing, date-format hint) left intact.
- **2026-06-03** — Show only tasks linked to the current discussion in the Tasks tab. `9b06c82`
  - _Why:_ Bug discovered during use
  - _Requested:_ במשימות צריך לראות את רשימת המשימות שמחוברות לדיון זה
  - _Done:_ Planned to make the Tasks tab show only the tasks linked to the current discussion. Root-caused that monday API 2025-04+ returns null for board_relation text and value, so the app saw zero links; fixed by reading relations via the typed linked_item_ids and linked_items fields (shared CV_FIELDS and cvSelection in monday-client), pinning API version 2026-04, and rewriting useTasks to fetch the discussion tasksBoardLink linked items directly (the connect column is bidirectional). Verified live: discussion 1 shows its 3 linked tasks grouped by status, and the app was deployed and confirmed in monday. Deviation: scope expanded from a simple tasks filter into an API-version regression fix, because server-side query_params filtering on board_relation columns matches by item name not id and never worked; relations are now read from the discussion side.
- **2026-06-03** — Stabilize first app launch by fixing a sequence of small runtime issues under one tracked change `5a4af17`
  - _Why:_ First production-like launch stabilization effort
  - _Requested:_ עכשיו האפליקציה עלתה לראשונה. אנחנו הולכים לתקן עכשיו רצף של תלקות קטנות, הכל יכול להיות בתוך שינוי אחד שהמטרה שלו היא להביא את האפליקציה לעבוד בפעם הראשונה
  - _Done:_ Planned to stabilize the first production-like run by fixing blocking runtime errors first, then iterating on small UI and configuration issues until the app was usable end to end. I fixed multiple Vibe API incompatibilities (removed invalid static enum usage), resolved host and modal behavior issues, and made the create flow and previous-instructions flow work with an explicit previous-discussion link. I also rebuilt the settings UX to match the tracker pattern, including accordion sections, searchable board/column pickers, type-filtered column selection, and updated mappings for discussions, tasks, and topics. Deviation: the work expanded from isolated crash fixes into a broader first-run hardening pass that included mapping-model redesign and settings UX restructuring, consistent with the approved umbrella stabilization goal.
- **2026-06-03** — Fix crash in DiscussionList when reading SMALL from an undefined object `bf3e6c1`
  - _Why:_ Bug discovered during runtime error investigation
  - _Requested:_ [13:37:38] [ERROR] [ErrorBoundary] Cannot read properties of undefined (reading 'SMALL') TypeError: Cannot read properties of undefined (reading 'SMALL') at DiscussionList (DiscussionList.jsx:101:35)
  - _Done:_ Planned to replace the unsupported TextField size constant in DiscussionList and verify that no additional runtime references remained. I updated the search field to use the supported literal prop size="small", removing the undefined SMALL access that triggered the ErrorBoundary crash. I verified the fix by linting the edited file, searching for additional TextField.sizes usages in runtime code, and running a successful production build. Deviation: the working tree also contained an earlier unrelated vite.config.js change, which was kept untouched because it was outside this bugfix scope.

### ✨ New Features

- **2026-06-29** — Redesign My Tasks Filter/Sort/Group dropdowns into monday.com's inline builder (column → condition) and add richer options: Label order/A→Z/Z→A for status & priority, deadline Earliest/Latest, task-name A→Z/Z→A, group-by-discussion A→Z/Z→A + Date ↑/↓ (lazy), a no-grouping state (single 'משימות' group), and deadline filtering (within Today/This week/This month/Overdue, before/after a specific date). English chrome, Hebrew column names, uniform field widths, monochrome icons, blue-bg/black-text selected pill, mobile bottom sheets; client-side over the loaded page. `0765b09`
  - _Why:_ Align My Tasks controls with monday.com's native UX and give users more capable, familiar sort/filter/group; approved via an interactive mock.
  - _Requested:_ Redesign the My Tasks toolbar Filter/Sort/Group dropdowns into monday.com's faithful inline builder (LTR sentence row: column → condition) with the full option set, deadline filtering, no-grouping state, English chrome, mobile bottom sheets.
  - _Done:_ Brought the approved Option A (monday-faithful inline builder) into the real MyTasksView, keeping all filtering/sorting/grouping client-side over the loaded page and lazy-loading discussion dates only when the discussion-date group order is chosen. Added a new controls/ subsystem (controls.js config + sortTasks/filterTasks helpers; BuilderControl + Segment + BuilderIcon; b-prefixed builder.module.css to avoid non-scoped class collisions), extended grouping.js to be order-aware with a no-grouping mode, rewired MyTasksView's state + filter→sort→group pipeline, and reused DatePickerPopover for the deadline before/after date. Verified via the production build, 163 passing tests (new controls/grouping/smoke tests), and a forced deploy to app 11457413 with the live CDN bundle hash confirmed. Deviations: the no-value bucket now sorts LAST (was first) and the unknown-mode grouping fallback is now 'status' (both deliberate consistency choices); added a default React import to MyTasksView for the jsdom test env; deploy needed --force since the latest version was already live (web/CDN updated, monday mobile may serve a cached bundle until a Dev-Center version bump).
- **2026-06-28** — Draggable + persisted per-column widths on the My Tasks table: shared infra (ColumnWidthsContext in monday.storage per-instance, useColumnWidths hook, ResizeHandle) wired into MyTasksTable. Owner-only drag (canManageSettings), name column resizable (min 200px), no reset; stored widths apply for ALL viewers, handles hidden on touch. Extensible to the discussion task tables next. `4aa2443`
  - _Why:_ User asked for the ability to resize table columns; decisions already captured in docs/column-widths-plan.md (per-instance, owner-only, name min 200, no reset)
  - _Requested:_ אי אפשרות לשנות רוחב עמודות של טבלאות
  - _Done:_ Plan: add CEO_Display-style draggable, persisted per-column widths to the tables, per docs/column-widths-plan.md and the owner decisions (per-instance storage, owner-only drag, name min 200px, no reset). Done: built shared infra — ColumnWidthsContext (monday.storage per-instance, ref-backed, debounced, non-blocking, mirrors TemplatesContext), a useColumnWidths(tableId, defs) hook that emits the grid-template string plus a startResize mouse handler, and a ResizeHandle component — and wired it into MyTasksTable so its columns are now fixed-px and draggable. Owner-only drag (canManageSettings threaded App -> MyTasksView -> MyTasksTable), name column resizable with min 200, no reset, handles hidden on touch; stored widths apply for all viewers. Added a unit test for the grid-template computation; 143 tests pass + build; deployed. Deviation: scoped the first wiring to the My Tasks table (TaskTable extension deferred to a follow-up); kept the width defs in a dedicated constants/columnWidths.js instead of boards.config.js.
- **2026-06-28** — Split the SettingsModal into two tabs: 'מיפוי' (the three board/column mapping accordions) and 'העדפות' (the previous-tasks source selection). Add a new preference toggle in Preferences to show/hide the 'My Tasks' tab, defaulting to OFF for backward compatibility; when off, App.jsx hides the Discussions⇄My-Tasks toggle and forces the discussions view. `037aae6`
  - _Why:_ Settings was a single long scroll mixing mapping and preferences; and the My Tasks tab should be opt-in per instance rather than always visible.
  - _Requested:_ חלוקת תיבת ההגדרות לשני טאבים — מיפוי (שלושת הלוחות) והעדפות (מקור משימות להנחיות קודמות), plus כפתור הצגה/הסתרה של טאב המשימות שלי, ברירת מחדל לא להציג (תאימות לאחור).
  - _Done:_ Planned to split the SettingsModal into 'מיפוי' (board/column mapping) and 'העדפות' tabs and add a showMyTasks preference (default OFF) that App.jsx respects. Wrapped the modal content in @vibe TabsContext (מיפוי = the three board accordions; העדפות = previous-tasks source + a new showMyTasks Toggle), added showMyTasks:false to DEFAULT_PREFERENCES, and gated the top-level My Tasks view toggle in App.jsx via useSettings (hidden + forced to the discussions view when off, even with a stale localStorage value). Extended in-session per user follow-ups: added an X clear button to each column-id SearchablePicker; hid unmapped non-critical columns (deadline/priority/notes) in the My Tasks table with a dynamically-built grid template; moved the discussion column to the end as a clickable connected-board chip (matching the 'דיון מקור' chip) that opens the discussion card; and reordered the Tasks/Previous-tasks toolbars so the primary action sits left and group-by + collapse-all sit right with collapse pinned to the edge.
- **2026-06-28** — Topics tab UX improvements: (1) invert the per-topic counter to show discussed/total (0/3 when none ticked, increments per tick) instead of remaining/total; (2) hidden topics/points are locked for editing, and a hidden topic's group renders collapsed; (3) the tab opens with all topics collapsed by default; (4) 'collapse all' button pinned right, icon-only (no label), with matching style in the Tasks and Previous-tasks tabs (not My Tasks). `96b428c`
  - _Why:_ The counter read backwards (showed full when nothing was discussed); hidden items should not be editable or expanded; and the collapse-all control was inconsistent and left-placed with text.
  - _Requested:_ שיפורים בטאב הנושאים: היפוך מונה לנדונו/סך, נעילת מוסתרים לעריכה + קיפול קבוצת נושא מוסתר, ברירת מחדל הכל מקופל, כפתור קפל הכל צמוד לימין אייקון בלבד כולל אחידות בטאבי משימות והנחיות קודמות.
  - _Done:_ Planned Topics-tab UX fixes: invert the per-topic counter to discussed/total, lock hidden topics/points from editing with hidden topics force-collapsed, default the tab to all-collapsed, and make 'collapse all' an icon-only button pinned right consistently across Topics/Tasks/Previous-tasks (not My Tasks). Inverted the counter to discussedCount/total; gated rename and the 'discussed' toggle on hidden items and force-collapsed hidden topics via an effectiveOpen flag; added a one-time per-discussion default-collapse effect (new topics still open); and replaced the collapse-all text buttons with icon-only @vibe/icons IconButtons (Collapse/Expand) pinned to the right edge in all three tabs. No deviations from the approved plan.
- **2026-06-28** — When creating a task in 'previous tasks by discussion type' mode, auto-create the discussion-type label on the tasks board's taskTypeID column if it's missing, instead of skipping the stamp. New createLabelsIfMissing option on BoardSDK create/update emits monday's create_labels_if_missing; useTasks.createTask writes the type by text. Always-on, no settings toggle. `96b428c`
  - _Why:_ Tasks created from a discussion whose type label did not exist on the tasks board were silently left untagged and never appeared in the 'by discussion type' previous-tasks view.
  - _Requested:_ הוספת דגל createLabelsIfMissing — ביצירת משימה במצב משימות קודמות לפי סוג דיון, אם הלייבל של סוג הדיון לא קיים בעמודת taskTypeID בלוח המשימות, הוא ייווצר אוטומטית במקום לדלג על החותמת.
  - _Done:_ Planned an always-on createLabelsIfMissing capability so creating a task in 'previous tasks by discussion type' mode stamps taskTypeID by the parent discussion's type label TEXT and auto-creates that label on the tasks board when it's missing. Added a createLabelsIfMissing option to BoardSDK create/update (emits monday's create_labels_if_missing), extended formatValue to write a status column by label text via { label }, and changed useTasks.createTask to stamp taskTypeID by text with the flag. Folded in several user follow-ups in the same area: hid the 'move to next discussion' action in by-type mode, made the task name open its item card on the Updates pane from the Previous-tasks tab, removed the Settings subtitle and renamed the heading to 'מקור המשימות בהנחיות קודמות', and unified the 'source discussion' chip to open via kind:'updates' — which (verified live) makes it open as a side panel like the task card rather than a modal.
- **2026-06-28** — Add 'Previous tasks by discussion type' view: new tasks-board status column (taskTypeID) auto-filled with the discussion's type on task creation (mapped by label text), a Settings mode selector (by linked discussion / by discussion type), and a server-side type filter in the Previous Tasks tab. Only new tasks get classified. `09dec87`
  - _Why:_ Let users see all tasks of a given discussion type as previous instructions, not only tasks reachable via the discussion-to-discussion link
  - _Requested:_ הוספת תצוגת הנחיות קודמות לפי סוג דיון: עמודת סטטוס חדשה בלוח המשימות (taskTypeID) שמתמלאת אוטומטית בסיווג הדיון בעת יצירת משימה (מיפוי לפי טקסט הלייבל), בורר מצב בהגדרות (לפי דיון קודם / לפי סוג דיון), וטאב הנחיות קודמות שיודע לסנן server-side לפי הסוג. רק משימות חדשות יקבלו סיווג.
  - _Done:_ Plan: Add a second resolution mode to the Previous Tasks tab — show all tasks sharing the current discussion's type — via a new tasks-board status column (taskTypeID) auto-stamped on task creation, a Settings mode selector, and a server-side type filter. Done: Added taskTypeID to schema + Settings; useTasks stamps every new task with the parent discussion's type, bridging the two independent status columns by label text; added a previousTasksMode preference (ButtonGroup) under settings.preferences; PreviousTasksTab filters the tasks board server-side by type in by-type mode, with a connected-board-style 'דיון מקור' chip (white, full-width, opens the item card) and a group-by-source-discussion option. Deviations: Beyond the original plan, fixed a BoardSDK bug where status where-filters sent a string compare_value (monday returns zero matches — verified live; must be the numeric label index), and added the source-discussion column, clickable chip, and grouping per follow-up requests. Commit also carries pre-existing uncommitted My Tasks/CSS polish that was already in the working tree.
- **2026-06-18** — Limit every task-assignee field to a single person: add a 'single' prop to PersonPicker that blocks picking a second person (popover stays open so the user can deselect first) and shows a monday notice 'ניתן להקצות אחראי אחד בלבד'. Enabled on all task-assignee pickers: TaskTableRow, NewTaskModal, TaskCard. Enforced going forward only; existing tasks with multiple assignees untouched. Discussion lead/participants and participant templates unaffected. `33d823e`
  - _Why:_ User wants each task to have exactly one responsible person, with an alert when someone tries to add more.
  - _Requested:_ אני רוצה שבכל שדות האחראי משימה, יהיה אפשר לשים אדם אחד בלבד, ותהיה התראה אם לא
  - _Done:_ Planned to add a 'single' prop to the shared PersonPicker that caps task-assignee selection at one person — blocking a second pick and surfacing a Hebrew monday notice — then enable it on every task-assignee picker. Implemented 'single' in PersonPicker: when a person is already selected, a second pick is blocked, the popover stays open so the user can deselect first, and monday.execute('notice', …) shows 'ניתן להקצות אחראי אחד בלבד' (wrapped in try/catch with a logger.warn fallback). Enabled the flag on TaskTableRow (covering TasksTab, PreviousTasksTab, and EffectivenessTab, including batch assignee updates), NewTaskModal, and TaskCard; the discussion lead/participants and participant-template pickers were left as multi-select, and existing tasks with multiple assignees were not modified (enforcement is forward-only). All 110 tests pass; no deviations from the plan.
- **2026-06-09** — Add a persisted in-memory users/avatars store (src/utils/usersStore.js): account roster {id,name,photo_thumb} cached in a module singleton + monday.storage, hydrated instantly on boot. A silent full-roster sync runs when an admin/board-owner connects (App.jsx). useUsers, PersonAvatar/PersonList and PersonPicker all read from the shared store (synchronous, cache-first) with an on-demand ensureUsers() fallback for missing ids. Removes the duplicate per-component caches and the console.error in use-users.js. `bd38c36`
  - _Why:_ Avatar photos took time to load every session (per-id fetch, ephemeral cache, picker loaded roster only on open). A persisted store + manager-driven silent sync makes avatars appear instantly, like the Planner app.
  - _Requested:_ אני רואה שלתמונות אווטרים לוקח זמן להיטען. אני רוצה זה יהיה אובייקט בזיכרון של האפליקציה, שמבצע סינכרון שקט כאשר מנהל מתחבר, כמו באפליקציית planner
  - _Done:_ The approved plan was to add a module-level in-memory users/avatars store persisted to monday.storage, with a silent full-roster sync when an admin connects, so avatars appear instantly across sessions. usersStore.js was shipped inside bd38c36: a Map singleton hydrated from monday.storage on boot (hydrateFromStorage), ensureRoster() for the full account roster (deduped with a single in-flight promise), and ensureUsers(ids) for on-demand single-user fallbacks. PersonPicker reads the store synchronously via getUser(id) and calls ensureRoster on open. subscribe()/getVersion() pair lets components re-render via useSyncExternalStore when the cache changes. A follow-up (8c57015) fixes single-assignee display in PersonPicker: one person now renders a plain Avatar instead of AvatarGroup, since AvatarGroup's stacking padding was shifting the avatar off-center.
- **2026-06-09** — NewTaskModal: require assignee+deadline with inline validation; tab-aware create in DiscussionCard (not-on-Tasks-tab => loader toast then success; on-Tasks-tab => close immediately); fix missing loader+toast on carry-forward in PreviousTasksTab.moveSelectedToNext; EffectivenessTab renders filtered tasks as TaskTable rows (no add-task row) instead of TaskCard `8c57015`
  - _Why:_ Prevent tasks without owner/deadline; clear creation feedback from any tab; consistent visible feedback on carry-forward; consistent task presentation matching the Tasks board
  - _Requested:_ אם יוצרים משימה מתוך מודול משימה חדשה, כל עוד אנחנו לא בטאב משימות יש להציג loader toast ואז הצלחה. אם אנחנו בטאב משימות יש לסגור מיד את התיבה. מתוך התיבה, תאריך דדליין ואחראי הם חובה על מנת ליצור משימה ויש להציג הודעה מתאימה אם חסרים. ובדוק מדוע טוען וטואסט לא מוצגים בהעברה של משימה לדיון הבא. בטאב אפקטיביות תציג שורות טבלה כמו הקומפוננטה בלוח משימות, אין צורך בשורה של משימה חדשה
  - _Done:_ The approved plan was to require assignee and deadline in NewTaskModal with inline validation, make DiscussionCard tab-aware when creating tasks, fix the missing loader+toast on carry-forward in PreviousTasksTab, and render EffectivenessTab tasks as TaskTable rows. Shipped in commits 467fc7e and 89b7e27: NewTaskModal blocks submit and shows inline error messages when assignee or deadline are missing; DiscussionCard detects the active tab — on Tasks tab it closes the modal immediately, on any other tab it shows a loader toast then a success notice; PreviousTasksTab.moveSelectedToNext now shows the loader+toast flow. EffectivenessTab renders filtered tasks as TaskTable rows (no add-task row). A follow-up commit (8c57015) restricted the new-task FAB to the topics tab only, so it no longer floats over Summary/Tasks/Effectiveness.
- **2026-06-07** — Add a Summary tab with a minimal RTL rich-text editor (TipTap) saving HTML as a single editable monday Update on the discussion item `33754aa`
  - _Why:_ User request — a place to write a free-text discussion summary stored as a monday update
  - _Requested:_ i want to add a place to free text discussion summary. minimal rich text editor, save to HTML, save it to an update, in a new tab. Approach B: single editable monday Update via TipTap; conservative toolbar; editor-only; all users can edit.
  - _Done:_ The approved plan was to add a 'סיכום' tab with a minimal RTL rich-text editor (TipTap, Approach B) that saves/loads a single editable monday Update per discussion. The full feature shipped across several commits: TipTap editor with a conservative toolbar (bold, italic, underline, strikethrough, text color, link, alignment, checklist); debounced auto-save with a small status indicator in the corner; unauthorized-save shows a clear Hebrew message; the saved HTML fully preserves rich formatting including monday-style checklists. The docx export was extended to include the summary HTML rendered as Word paragraphs, and later polished: Figtree/Noto Sans Hebrew brand fonts applied to the Word document, tasks table gained a '#' row-number column and cantSplit rows, metadata lines use bold labels, the title is prefixed 'סיכום דיון:', and the 'סוג' status label id is resolved to its text at export time via fetchTypeLabel.
- **2026-06-05** — Discussion card + previous-tab polish: show all participant avatars in the card header (fetch column6 in the list query); single gray line under the tabs (removed colored indicator); uniform fixed-width status pills (custom pill instead of Vibe Label); previous-tasks tab shows a loader until the link resolves, a centered 'no previous discussion' state with a picker to set one, and a styled RTL 'דיון קודם' chip
  - _Why:_ Fix missing participant avatars, double tab divider, inconsistent status-label widths, and the previous-discussion UX (flash of empty state, no way to set it, awkward LTR header)
  - _Requested:_ Show participants' avatars top-right of the card header; one gray line under the tabs (not two); uniform status-label width; on opening a discussion show a loader (not an instant 'no previous'), and if none, a centered message + button opening a dropdown to pick a previous discussion; restyle the 'previous discussion' header to suit Hebrew under LTR.
  - _Done:_ Plan: surface participant avatars in the card header, collapse the double tab divider to one gray line, make status pills uniform width, and rework the previous-tasks tab (loader-first, picker when none, styled Hebrew chip). Done: useDiscussions list query now also fetches column6 so the card header renders all participant avatars (PersonList max = participants count); discovered withColumns DOES limit fetched columns (the CLAUDE.md note was stale). Removed the custom colored indicator under the tabs and set the header padding-bottom to 0 so the tabs sit flush on the single gray header border. Rewrote StatusBadge as a fixed-min-width custom pill (Vibe Label's inner element ignored the wrapper min-width). PreviousTasksTab: added a 'resolving' state so it shows a loader until the previous-discussion link is resolved; when none, a centered 'לא הוגדר דיון קודם' with a Dropdown picker that writes the previousDiscussion relation onto the current discussion; restyled the header as a dir=rtl pill chip ('דיון קודם' + name). No deviations.
- **2026-06-05** — EffectivenessTab chart rework: vertical column chart (X=status, Y=task count); toggle to group by assignee (X=assignee, Y=count, stacked by status); clicking a bar selects+scrolls to the task list below; remove the chart title and the click-hint caption
  - _Why:_ Make the tasks distribution chart clearer and more useful (column layout, assignee breakdown, direct scroll-to-list)
  - _Requested:_ Column chart for tasks: clicking a column scrolls to the task list below. Remove the 'click a column to view tasks' caption. X axis = task status, Y axis = number of tasks. Option to break down by assignee: X = assignee, Y = task count, stacked by status. Remove the 'status distribution' chart title.
  - _Done:_ Plan: vertical column chart (X=status, Y=count) with a by-assignee toggle (stacked by status), click-a-bar scrolls to the task list, and remove the title + click-hint caption. Done: reworked EffectivenessTab to a vertical recharts column chart; added a status/assignee segmented toggle (assignee mode stacks status segments per assignee, keyed by joined assignee names like TasksTab); clicking a bar selects the status/assignee and scrolls to the filtered task list (scrollIntoView); removed the 'התפלגות סטטוס' title and the 'לחץ על עמודה לצפייה במשימות' caption. No deviations.
- **2026-06-05** — Tasks table: inline rename of a task name (click/double-click to edit, save on Enter/blur); new-task row collapses to a + button that opens an inline text field on click (removes the always-visible placeholder field)
  - _Why:_ Let users rename tasks in place and make the add-task affordance cleaner/less cluttered
  - _Requested:_ In the tasks table (TaskTable / TaskTableRow): allow renaming a task name inline (click/double-click the name to edit, save on Enter/blur). And change the new task row so it shows just a + button; clicking it opens an inline text field to type the task name (remove the always-visible placeholder field).
  - _Done:_ Plan: inline task rename + collapse the add-task row to a + that expands into a field. Done: added updateTaskName to useTasks (.update({name}), optimistic); TaskTableRow name cell is inline-editable when onRenameTask is provided (click/double-click → input, save on Enter/blur, Esc cancels); TaskTable add row is now a + button that expands into an input (Enter adds, Esc/blur-empty collapses); TasksTab passes updateTaskName to both TaskTable usages. Read-only tables stay read-only. No deviations.
- **2026-06-04** — TopicsTab: drag-and-drop reorder of topics and points, inline rename of a topic/point, delete a topic/point, and continuous point entry (Enter opens a new empty point input)
  - _Why:_ Give users full in-UI management of discussion topics and points instead of add-only
  - _Requested:_ In the discussion Topics tab (TopicsTab): add drag-and-drop reorder of topics and points, inline edit (rename) of a topic and a point, and delete a topic or a point. Also: pressing Enter after typing a point under an open topic should immediately open a new empty point input so points can be added continuously.
  - _Done:_ Plan: drag-reorder, inline rename, and delete for topics and points + continuous point entry. Done: added renameTopic/deleteTopic/renamePoint/deletePoint/reorderTopics/reorderPoints to useTopics (rename via change_multiple_column_values 'name' key, delete via delete_item, optimistic). monday has no item-position API, so order is persisted per-discussion in monday.storage (new utils/topicOrder.js) and re-applied on each fetch. Rebuilt TopicsTab with dnd-kit (grip handles, inline title rename, inline delete-confirm) and made TopicPointRow sortable with inline rename+delete; Enter on a new point keeps the input open for continuous entry. Deviation: the new-point field uses a plain input instead of Vibe TextField so focus survives rapid Enter-adds.
- **2026-06-04** — Add participant templates (new tab in templates modal + apply-from-template control on the participants picker in the create dialog) and a duplicate-discussion button next to edit in the list (opens the create dialog pre-filled with the source's fields and topics, empty date, name selected for editing)
  - _Why:_ Let users reuse fixed sets of participants across discussions and quickly recreate recurring discussions without re-entering details
  - _Requested:_ Add participant templates (new tab in the templates modal to define reusable sets of participants, and an apply-from-template control on the participants picker in the create-discussion dialog) and a duplicate discussion button next to the edit button in the discussion list (opens the create dialog pre-filled with the source discussion's fields and topics, with an empty date and the name selected for editing).
  - _Done:_ Plan: add a participant-templates tab to the templates modal + an apply-from-template control on the participants picker, and a duplicate-discussion button seeding the create dialog with the source's fields and topics. Done: added a parallel participant-templates store in TemplatesContext (separate monday.storage key) with full CRUD; added a נושאים/משתתפים tab bar to TemplateManagerModal with a PersonPicker-based participant editor; added a 'מתבנית' merge control beside the participants picker in CreateDiscussionModal; added a duplicate (Copy) button in DiscussionList that opens the create dialog seeded from the source (name/description/lead/participants + resolved previous-discussion link, empty date, name focused & selected) and clones the source's topics+points after creation. No deviations.
- **2026-06-03** — Reusable topic+point templates: manager modal, apply in new and existing discussions, per-instance storage `b9890ca`
  - _Why:_ New product feature
  - _Requested:_ (retroactively logged during #22 close — templates feature found uncommitted in the working tree)
  - _Done:_ Added a reusable topic+point templates feature. A TemplateManagerModal lets any user create and edit templates (a named set of topics, each with discussion points), persisted per-instance via monday.storage in TemplatesContext; templates can be applied when creating a new discussion (a dropdown in CreateDiscussionModal) or onto an existing discussion (an ApplyTemplateMenu in the Topics tab). Core logic lives in src/utils/templates.js (createTopicsFromTemplate, countPoints) with unit tests under src/utils/__tests__. Note: retroactively logged and committed during the #22 LTR close, as the work was found uncommitted in the working tree.

### 🔧 Feature Changes

- **2026-06-11** — Week calendar: remove the 'all-day' row, limit the hour range to 06:00-23:00 (opens on 07:00), and make hour selection mandatory when creating a discussion (a 'שעה' label with a red asterisk matching 'תאריך', plus blocking validation). `a333034`
  - _Why:_ Cleaner scheduling UX — every discussion must be assigned a concrete hour; the all-day row is no longer needed.
  - _Requested:_ לוח השבועי בדיונים: הסרת שורת כל היום, צמצום טווח השעות ל-6:00-23:00 עם פתיחה ב-07:00, והפיכת בחירת השעה לחובה ביצירת דיון חדש.
  - _Done:_ The approved plan was to remove the 'all-day' row from the week calendar, limit the visible hour range to 06:00-23:00 (opening at 07:00), and make hour selection mandatory when creating a discussion. WeekGrid drops the all-day strip and compresses the grid from 24 to 18 hours (height 864px); topForMinute() anchors all positions to the 06:00 baseline. CreateDiscussionModal gains a 'שעה' label with a red asterisk, the time picker is restricted to 06:00-23:00 (no 'ללא שעה' option), and the submit button is disabled until a time is chosen. As necessary infrastructure for the calendar's accent colors, column23 was simultaneously migrated from dropdown→status in boards.config.js and provisionBoards.js; SettingsContext gained reconcileColumns() so already-configured instances get the updated type; useStatusOptions was generalized with boardKey/alias params; discussionAccentColor() derives the dot/chip color from the discussion's status label color; DiscussionList, OverviewTab, EventChip, and DiscussionCalendar all consume the real status color. 'סוג' in CreateDiscussionModal switched from multi-select Chips to a single-select status dropdown with color swatch, and selectType() auto-attaches the topic/participant template linked to that type on new discussions.
- **2026-06-04** — Redesign create/edit discussion modal: inline-editable title (auto-selected for new), creator (read-only)+date row, discussion-lead+participants people fields (new mapped columns), previous+template row, description last, left-aligned `bd38c36`
  - _Why:_ UX redesign to the desired layout + add discussion lead/participants fields
  - _Requested:_ פה אני רוצה לראות: למעלה במקום עריכת דיון, את שם הדיון בשדה טקסט ניתן לעריכה. עבור דיון חדש יהיה כתוב דיון חדש ובפתיחה הטקסט יהיה מסומן. מתחת יוצר דיון (לא ניתן לשינוי) ותאריך באותה שורה. מתחת 2 שדות למוביל דיון ומשתתפים כמו עמודת people, מתחת דיון קודם ותבנית נושאים בשורה אחת ובסוף תיאור. תוודא שהכל מיושר לשמאל.
  - _Done:_ The approved plan was to redesign the create/edit discussion modal with an auto-selected inline title, a creator+date row, separate discussion-lead and participants people fields, a previous-discussion and template row, and a description field at the bottom — all left-aligned. bd38c36 implements the full layout: editable title (auto-selected for new, pre-filled for edit/copy), read-only creator avatar, date+time row, two PersonPicker rows for lead and participants, previous-discussion dropdown, and template-apply dropdown. Added discussion-type chips via a dropdown column (column23), toast feedback after create, and a copy-discussion shortcut that pre-fills the new modal. No significant deviations from the approved plan.
- **2026-06-03** — Discussions view improvements: edit an existing discussion, collapsible/hideable discussions sidebar, and fix clipped dropdowns `56407b5`
  - _Why:_ Usability improvements + a clipping bug found while testing
  - _Requested:_ צריך אפשרות לערוך פרטים של דיון קיים. לצמצם ולהסתיר את עמודת הדיונים, ושים לב שהדרופדאונים נחתכים
  - _Done:_ Planned three discussions-view improvements: edit an existing discussion, a collapsible discussions sidebar, and a fix for clipped dropdowns. Added an in-list edit pencil that opens the create form in edit mode (prefilled name/date/description/previous-discussion, with clearing, search, and white-background dropdowns), a desktop collapse toggle that hides the sidebar for a full-width card, plus form spacing and an LTR selected-accent fix. Deviation: the edit affordance moved from the card header into each list row and reused CreateDiscussionModal instead of a separate modal per user feedback; BoardSDK.update() was extended to rename items and clear board_relations. Deviation: the clipped-dropdowns root cause was not overflow but the sticky Vibe TableHeader z-index covering the date/status popovers, fixed with zIndex={1000}.
- **2026-06-03** — Use settings-driven discussion column fetching and add JSON export for settings. `2fca4e4`
  - _Why:_ Improve reliability and transparency of settings-based mappings.
  - _Requested:_ אל תעשה שליפה דו לשבית. פשוט תשלוף את כל הערכים בעמודות מלוח דיונים לפי ההגדרות עצמן. תוסיף אפשרות לייצוא אובייקט ההגדרות בJSON
  - _Done:_ Planned to fetch discussion columns from the settings mapping (no two-way relation scan) and add a JSON export of the settings object. Removed all hardcoded board and column ids; boards.config became a type and title schema only, board-config-store starts empty, and SettingsContext loads the mapping solely from monday.storage (forcing the Settings modal when unconfigured); BoardSDK.withColumns now narrows fetched columns so the discussions list stays light. The settings JSON export was added to SettingsModal. Deviation: expanded into a full storage-only settings model with no defaults, per the instruction to never hardcode ids and rely only on settings.
- **2026-06-03** — Update Topics tab to show linked topic groups and their subitems as discussion points; remove prefilled list from new topic creation. `d1533b2`
  - _Why:_ Improve UX and data consistency with board structure
  - _Requested:_ בנושאים צריך לראות את רשימת הנושאים המחוברים בעמודת נושאים לדיון. כשל נושא הוא בקבוצה, וכל הסאב אייטמים שלו הם נקודות לדיון מתחת הנושא. וצור נושא חדש לא צריך להכיל רשימה מראש
  - _Done:_ Planned to show topics linked via the discussion topics relation, with each topic as a section and its subitems as discussion points, and to drop the prefilled list from new-topic creation. Rewrote useTopics to read the discussion topicsBoardLink linked topic items and their subitems (points), made TopicsTab render each topic as its own collapsible section with an add-point button, and repurposed TopicPointRow as a point row with a discussed checkbox plus create-task. Added a checkbox column (boolean_mm3z7wrp) to the subitems board and wired per-point toggling (auto-detected, also mappable via the new pointChecked setting). Deviation: the model was corrected twice mid-work, first away from native-group grouping, then to topic=item and points=subitems per the user clarification, and a subitems-board checkbox column had to be created since the subitems board had none.

### 🎨 Design

- **2026-06-29** — Fix the My Tasks toolbar active-pill blue background being clipped top/bottom on mobile: the toolbar's overflow-x:auto forced overflow-y to auto (CSS spec), which vertically clipped the pills. Since the icon-only pills fit one row, drop the horizontal-scroll overflow entirely and make each mobile pill a 32x32 square with the icon centered, so the active (blue) background is a fully-visible rounded square with equal margins around the icon. `580697f`
  - _Why:_ User: the selected pill's blue background is cut at top and bottom; wants equal margins on all sides around the icon. Fine when groups are collapsed, clipped when expanded.
  - _Requested:_ הרקע הכחול של הכפתור הנבחר לא ייחתך למעלה ולמטה; שוליים שווים לכל הכיוונים סביב האייקון. תקין כשהקבוצות סגורות, חתוך כשנפתחות
  - _Done:_ Plan: stop the My Tasks toolbar active-pill blue background from being clipped top/bottom on mobile and give the icon equal margins. Done: root cause was the toolbar's overflow-x:auto, which per CSS spec forces overflow-y to auto and vertically clipped the pills. Since the icon-only pills fit one row, removed the horizontal-scroll overflow (and nowrap) entirely, and made each mobile pill a 32x32 square with the icon centered (padding 0, justify-content center) — so the active blue background is a fully-visible rounded square with equal margins on all sides around the icon. CSS-only in MyTasksView.module.css. Verified 143 tests + build; deployed via deploy:force (hash verified).
- **2026-06-29** — Mobile round 4: (1) keep the discussion-card info button on the SAME line as the title (titleRow nowrap, title wraps internally) and hide the link-copy button on mobile; (2) make the frozen name-column shrink GRADUAL/proportional to horizontal scroll (50vw -> 25vw, 1:1 with scrollLeft via a --name-col CSS var set on scroll) instead of a jump, in both tables; (3) fix the My Tasks toolbar being cut off by the group tables — bump it above them (z-index 20, drop the fragile negative margins) so the sticky toolbar always covers the tables; (4) make the toolbar pills icon-only on mobile (hide the label/count text spans, keep the svg icon) `ac03803`
  - _Why:_ User mobile feedback: info button wrapped below the title + wants link-copy gone; the half->quarter name shrink jumped instead of tracking the scroll; the search/filter/group toolbar was clipped from below by the table when groups were open (fine when collapsed); and pills should show icon only, no text
  - _Requested:_ כפתור מידע בשורה אחת עם שם הדיון; להסיר כפתור העתקת לינק; מעבר עמודת השם מחצי לרבע הדרגתי עם הגלילה (לא בקפיצה); כפתורי הסינון/חיפוש/קיבוץ חתוכים מלמטה ע"י הטבלה (תקין כשהקבוצות סגורות); ובכפתורים רק אייקון ללא מלל
  - _Done:_ Plan: fourth mobile round from user feedback — keep the card info button on the title line and drop the link-copy button on mobile, make the frozen name-column shrink gradually with scroll instead of jumping, fix the My Tasks toolbar being clipped by the group tables, and make the toolbar pills icon-only. Done: (1) DiscussionCard titleRow is nowrap on mobile so the back-arrow + title + info button stay on one row (the title wraps internally via titleBlock min-width:0), and the link-copy IconButton is hidden on mobile (kept on desktop). (2) The frozen name column is now driven by a --name-col CSS var that the table's onScroll handler sets to max(25vw, calc(50vw - scrollLeftpx)) — a smooth, 1:1-with-scroll shrink from half-screen to quarter-screen, in BOTH MyTasksTable (inline grid template) and the shared TaskTable (mobile grid var); removed the previous binary scrolled-state jump. (3) The My Tasks toolbar is sticky with z-index:20 (above the tables' z-index 2-3 frozen cells) and no longer uses fragile negative margins, so an expanded group table can't clip its bottom edge. (4) Toolbar pills are icon-only on phones — hide the label/count text spans; @vibe/icons render as a direct <svg> (not a span) so the icon stays. Verified 143 tests + build; deployed via deploy:force with the live hash verified.
- **2026-06-29** — Mobile round 3: (1) make the My Tasks toolbar STICKY so search/filter/group stays visible while scrolling the list (it was scrolling out of view); (2) flip the discussion-card back arrow to point the correct way (MoveArrowLeft); (3) on mobile replace the header date+participants with an info icon button that opens a popover showing date/time/participants; (4) dynamic frozen name-column width across My Tasks + the discussion TaskTable — half-screen (50vw) when not horizontally scrolled, quarter-screen (25vw) once scrolled, via a scroll-state listener `5eff305`
  - _Why:_ User mobile feedback: toolbar still not visible (scrolls away), back arrow points the wrong way, header date/participants clutter the title, and the frozen name column hides half the screen during horizontal scroll so you can't see which task the scrolled values belong to
  - _Requested:_ להפוך חץ חזרה; תאריך+משתתפים לכפתור מידע (אייקון) שפותח מודל; בגלילה אופקית שם המשימה יצטמצם (חצי מסך ללא גלילה, רבע בגלילה); ובמשימות שלי לא רואים את תפריט החיפוש/סינון/קיבוץ + להרחיב שם המשימה כמו שאר הטבלאות
  - _Done:_ Plan: third mobile round from user feedback — make the My Tasks toolbar sticky, flip the card back arrow, replace the header date/participants with an info-button popover on mobile, and make the frozen name column half-screen at rest / quarter-screen when scrolled across both tables. Done: the My Tasks toolbar is now position:sticky top:0 (negative margins span the .root 16px padding) so Search/Filter/Sort/Group stay pinned while the list scrolls — it had been scrolling out of view; the discussion-card back arrow now uses MoveArrowLeft (was pointing the wrong way); on mobile the date + participants collapse behind an Info IconButton that opens a click-outside popover showing date/time/participants (desktop keeps the inline date + avatars); and both MyTasksTable and the shared TaskTable track horizontal-scroll state (scrollLeft > 8px) to size the FROZEN name column 50vw at rest and 25vw once scrolled — MyTasks via its inline grid template, TaskTable via a --name-col CSS var toggled by a .scrolled class plus compact mobile column widths. Verified 143 tests + build; deployed via deploy:force with the live hash verified.
- **2026-06-29** — Mobile fixes round 2: (1) revert My Tasks toolbar to label pills on one scrollable row (icon-only had hidden the toolbar); (2) shrink the mobile My Tasks name column to <1/4 screen (22vw) with horizontal scroll; (3) FROZEN name column in ALL discussion task tables via shared TaskTable (covers Previous-instructions, Tasks, Effectiveness) — remove overflow:hidden, sticky name (+ checkbox when selectable), opaque cell + group strip as inset shadow; (4) redesign the discussion-card header on mobile so the title isn't squeezed to 3 lines next to the date/avatars (stack participants below) `e76e076`
  - _Why:_ User mobile feedback: toolbar missing/unusable, status color bleeding under the name, name column too wide on horizontal scroll, frozen column wanted in all discussion tables, and the discussion title wrapping awkwardly in the card header
  - _Requested:_ חסר הסרגל העליון של סינון וקיבוץ; צבע עמודת סטטוס מתחת לשם; בגלילה אופקית שהשם יצטמצם לפחות מרבע מסך; עמודה קפואה בכל הטבלאות (הנחיות קודמות, משימות, אפקטיביות); ובתצוגת הדיון הכותרת יורדת למטה — לתכנן מחדש
  - _Done:_ Plan: second mobile round from user feedback — fix the My Tasks toolbar, shrink the mobile name column, freeze the name column in the discussion task tables, and stop the discussion title squeezing in the card header. Done: reverted the My Tasks toolbar to label pills on one nowrap + horizontally-scrollable row (the earlier icon-only attempt had hidden the @vibe/icons wrappers and blanked the toolbar); set the mobile My Tasks name column to 22vw (< 1/4 screen) with horizontal scroll; added a FROZEN sticky name column to the shared TaskTable — covering the Previous-instructions, Tasks and Effectiveness tabs — by removing overflow:hidden from .taskTable, making .taskFirst (and .selectCell in selection mode) position:sticky + opaque, and moving the group-color strip to an inset shadow so it travels with the frozen column instead of scrolling away; and stacked the discussion-card header on phones (flex-wrap) so the title takes the full width and the date/avatars wrap to a row below. Verified 143 tests + build; deployed via deploy:force (hash verified live). Note: confirmed the frozen cell is already opaque so there is no real status bleed — the colored strip the user saw is the by-status group accent; the cross-file .bodyRow:hover .taskFirst rules are dead due to CSS-module scoping but harmless since the cell is opaque white at rest and hover.
- **2026-06-28** — Mobile toolbar/tabs/columns polish: (1) My Tasks toolbar fits ONE row on phones — pills go icon-only (label <span> hidden, icon + count kept) with nowrap+scroll fallback; (2) discussion-card tab strip now scrolls horizontally on mobile — root cause was the Vibe tab list (block-level flex) filling 100% and squishing the tabs, fixed with width:max-content so it overflows and .tabsRow scrolls; (3) compact fixed column template for the My Tasks table on phones so name+status+date are readable (the #112 resizable 400px name was filling the whole viewport, showing only the name column) `4aa2443`
  - _Why:_ User reported the My Tasks toolbar wrapped to 2 rows and the discussion tabs were unsuitable on mobile; the #112 resize feature also regressed the mobile My Tasks table to show only the name column
  - _Requested:_ הסרגל העליון של חיפוש/סינון/פילטר מופיע ב-2 שורות; ובתצוגת דיון סרגל הטאבים מופיע בצורה לא מתאימה לנייד
  - _Done:_ Plan: mobile polish for the My Tasks toolbar (wrapping to 2 rows), the discussion-card tab strip, and the mobile table columns. Done: made the My Tasks toolbar icon-only on phones (hid the label spans, kept the icon + count; nowrap + horizontal-scroll fallback) so all controls fit one row; fixed the discussion-card tab strip to scroll horizontally on mobile — root cause was the Vibe tab list being a block-level flex row that filled 100% and squished the tabs, fixed with width:max-content so it overflows and .tabsRow scrolls; and added a compact fixed column template for the My Tasks table on phones so name + status + date are readable instead of the desktop 400px name filling the viewport. Verified 143 tests + build; deployed via deploy:force.
- **2026-06-28** — Phase 1 — shell & hybrid navigation: bottom-anchored primary action on mobile (thumb-reachable, safe-area), card tab-strip that scrolls horizontally instead of wrapping, smoother list<->card transition + a >=44px back button, settings-gear repositioning with safe-area, and reduced appViewBar padding on mobile `9c39a83`
  - _Why:_ Per docs/mobile-adaptation-plan.md: primary nav currently sits in the hardest-to-reach top corners, 5 card tabs don't fit narrow widths, list<->card flips abruptly, and the gear/collapse controls ignore notches and the 44px touch floor
  - _Requested:_ שלב 1 של התאמת הנייד — שלד וניווט היברידי, לפי docs/mobile-adaptation-plan.md. פעולה ראשית מעוגנת בתחתית בנייד, סרגל טאבים נגלל אופקית, מעבר list<->card חלק + כפתור חזרה >=44px, מיקום מחדש של גלגל ההגדרות עם safe-area, וצמצום padding בסרגל appViewBar בנייד.
  - _Done:_ Plan: Phase 1 of the mobile adaptation — a CSS-only (<=767px) pass on the shell & navigation: scroll the discussion card's tab-strip horizontally instead of wrapping, a >=44px back button, a smaller title, and a mobile-friendly appViewBar; bottom-anchoring deferred to Phase 3. Done: In DiscussionCard.module.css the 5-tab strip now scrolls on one line (overflow-x:auto + overscroll-behavior-x:contain), the back-button hit area is >=44px, the title shrinks 28px->22px, and header padding is reduced, all under a consolidated max-width:767px block. In App.module.css the view-toggle became a compact, content-width, centered segmented pill on mobile (replacing an initial full-width 52px band after the user's screenshots showed it didn't fit). In MyTasksView/MyTasksTable.jsx the notes + discussion columns were narrowed to ~1/3 and the frozen name column widened to minmax(260px,2fr). Verified build + 142 tests; deployed via deploy:force to app 11457413. Deviations: Two out-of-scope items were folded in at the user's request mid-flight — the My Tasks column-width re-proportioning, and re-doing the appViewBar mobile treatment as a compact pill after device screenshots. The bottom-anchored primary action half of the nav decision was deliberately deferred to Phase 3 to avoid colliding with the FAB; the settings-gear safe-area item was dropped (the floating gear is dead code — the real gear is inline in the list header). The commit also includes docs/column-widths-plan.md, a research doc for the next feature.
- **2026-06-28** — Full mobile compatibility — Phase 0: responsive infrastructure foundation (useViewport/useIsMobile hook over useVibeMediaQuery + context.mode, viewport-fit=cover, 100vh->100svh in shell + full-screen states, touch-action: manipulation on interactive controls) `0823aa9`
  - _Why:_ App renders desktop-only and looks broken on mobile, even though it already runs on the monday mobile app (board view + object). Goal: view and buttons adapted for mobile, per docs/mobile-adaptation-plan.md
  - _Requested:_ התאמה מלאה לנייד (mobile compatibility) לאפליקציית הדיונים — מימוש לפי docs/mobile-adaptation-plan.md. מתחילים בשלב 0: תשתית רספונסיביות רוחבית.
  - _Done:_ Plan: Lay the app-wide responsive foundation for the mobile-adaptation effort (per docs/mobile-adaptation-plan.md, Phase 0) — a single useViewport/useIsMobile hook, viewport-fit=cover, 100vh->100svh, and touch-action on tappable controls — without changing runtime behaviour yet. Done: Added src/hooks/useViewport.js combining Vibe's 767/1023 breakpoint line (@vibe/core useMediaQuery) with a soft read of MondayContext.mode==='mobile' (exported MondayContext for that soft read), backed by a 4-test unit spec; added viewport-fit=cover to the viewport meta; converted the app shell, the ErrorBoundary/NetworkErrorScreen/SetupWizard full-screen states and the boot loader from 100vh to 100svh with a vh fallback; and added touch-action: manipulation to buttons/links. Verified with 142/142 passing tests and a clean production build; desktop unaffected since the hook has no consumers yet. Deviations: the CSS breakpoint token was deferred (a custom property can't be read inside @media; the 767/1023 line lives in the hook and will be consolidated when CSS @media rules are unified in Phase 1), and the @media (hover:hover) helper was deferred to Phase 3 where the hover-only controls live.
- **2026-06-18** — Two UI polish fixes: (1) shorten the single-assignee notice to 'ניתן להקצות אחראי אחד בלבד' (drop the second sentence and the trailing period, which floated to the wrong side in RTL); (2) remove bold from task-table group titles (.groupTitle in TasksTab and PreviousTasksTab) — weight 700→400 including font-variation-settings 'wght', which was the actual cause of the bold rendering. `a28fe5c`
  - _Why:_ User feedback on the just-shipped single-assignee feature and the bold group titles: the notice period escaped right in RTL, and the group titles should not be bold.
  - _Requested:_ בהתראה תכתוב ניתן להקצות אחראי אחד בלבד, ללא נקודה בסוף שבורחת ימינה. תוריד את הבולד מהטקסט של כותרות קבוצה של טבלאות משימה
  - _Done:_ Planned two small UI fixes requested after shipping the single-assignee constraint: tweak the notice copy and de-bold the task-table group titles. Changed the PersonPicker notice to 'ניתן להקצות אחראי אחד בלבד' — dropped the second sentence and the trailing period (it rendered on the wrong side in RTL). Reduced .groupTitle in both TasksTab and PreviousTasksTab from weight 700 to 400, pinning font-variation-settings 'wght' to 400 to match (that forced axis was what actually rendered the titles bold). All 110 tests pass; no deviations.
- **2026-06-07** — Redesigned the task tables in the 'הנחיות קודמות' and 'משימות' tabs to monday board style: rounded grid table, left group color strip, bold/larger colored group titles, full-width status fill, status picker reading labels/colors from the status column settings_str (no hardcoded values, new useStatusOptions hook), vibe Person placeholder in a soft-shadow circle with no fill, monday Hebrew font, +2px text, date hover-clear X, and an app-styled group-by dropdown. `8c57015`
  - _Why:_ Make the discussion app's task tables match monday's native board look & UX (iterative user design feedback).
  - _Requested:_ עיצוב מחדש של טבלאות המשימות בטאבים הנחיות קודמות ומשימות לסגנון לוח monday; סטטוס מהגדרות העמודה; פלייסהולדר אנשים עם אייקון Person; גופן עברית של monday; פריסה ל-app 11457413.
  - _Done:_ The approved plan was to redesign task tables in the 'הנחיות קודמות' and 'משימות' tabs to monday board style. The redesign shipped across commits 9f5cdc7 and c4df006: rounded grid table, left-side group color strip, bold/larger colored group titles, full-width status cell with color fill read from the status column settings (new useStatusOptions hook — no hardcoded values), vibe Person placeholder in a soft-shadow circle with no fill, monday Hebrew font, +2px text. A follow-up commit (8c57015) moved the task creation date from an inline display beside the name to a hover tooltip, cleaning up the row. useStatusOptions was later generalized with boardKey and alias params so it can drive the 'סוג' status column color display as well.
- **2026-06-07** — Batch of small UI/design refinements (ongoing umbrella). First item: fix discussion-card header participant avatars — photos don't load (people fragment lacks text; photos unresolved) and render as empty circles; show them like monday's PEOPLE column with a small overlap (AvatarGroup) `a102c40`
  - _Why:_ Visual polish toward monday's native look; fix broken avatar rendering
  - _Requested:_ אנחנו הולכים לבצע המון שינויי עיצוב קטנים. כולם יהיו תחת שינוי אחד לתיעוד. ראשון: אווטרים של משתתפים בדיון - התמונות לא נמשכות, וצריך להציג כמו בעמודת PEOPLE עם חפיפה קטנה
  - _Done:_ The approved plan was an ongoing design umbrella covering avatar AvatarGroup overlapping photos in the discussion card header plus many follow-up refinements. Accumulated items shipped across multiple commits: discussion card participant avatars now use AvatarGroup with proper overlap; calendar header/sidebar separator rules aligned; section header rules inset with side margins; tab-rule continuity fixed (single continuous line under tabs); task name hover tooltip added; task tables got horizontal scroll on narrow layout; EventChip in month view now hides the time until hover; timed-chip hover stays solid instead of washing; the timed chip shows name+time on one row. The batch also includes: creation date moved from inline to a hover tooltip on the task name; FAB restricted to the topics tab; single-assignee in PersonPicker uses a plain Avatar (not AvatarGroup) for proper centering; self-hosted Figtree and Noto Sans Hebrew variable fonts wired up as the app font stack. No deviations — all items were incremental polish.
- **2026-06-07** — Redesign the discussions-column collapse/expand handle: smaller circle+arrow with solid white background; reveal the collapse arrow on hover anywhere over the discussions column (not only the divider strip); when collapsed leave a thin vertical strip near the screen edge with a permanently-visible expand arrow `76b5862`
  - _Why:_ Polish the collapse affordance to feel like monday's native pattern and make it easier to discover and use
  - _Requested:_ Redesign the discussions-column collapse/expand handle: smaller circle+arrow with solid white bg, reveal the collapse arrow whenever hovering anywhere over the discussions column (not just the divider strip), and when collapsed leave a thin vertical strip near the screen edge with a permanently-visible expand arrow.
  - _Done:_ The approved plan was to make the discussions-column collapse handle smaller and more discoverable: shrink the circle and chevron, give it a solid white fill, reveal it on hover over the whole column rather than only the thin divider strip, and turn the collapsed state into a thin edge strip with a permanently-visible expand arrow. Implemented in App.module.css (22px circle, 14px chevron, .dividerHandleHover reveal rule, and a .dividerHandleCollapsed strip with opacity:1 arrow) and App.jsx (a listHover state driven by onMouseEnter/onMouseLeave on the sidebar, plus IconButton size=xs/iconSize=14). Build passed and the change was deployed live. Two deviations: the standard deploy was blocked because the latest version was live, so deploy:force was run with the user's explicit approval; and visual verification was deferred to in-product (the handle is desktop-only and renders only with real monday context, which local dev lacks).
- **2026-06-05** — UI polish: center the tasks-table column headers (אחראי/דד ליין/סטאטוס) over their columns + center their cell content; fix spelling to 'סטאטוס' app-wide; PersonPicker search field uses a monday Search icon instead of placeholder text; DatePickerPopover shows only a calendar icon when empty and only the date when set (no clear-X, no calendar icon)
  - _Why:_ Visual consistency and cleaner affordances per user review
  - _Requested:_ Center column headers (אחראי/דדליין/סטאטוס) over their columns in all tabs; spell 'סטאטוס' everywhere (not 'סטטוס'); in PersonPicker replace the 'חיפוש שם...' placeholder with a monday search icon; date picker placeholder should be only a calendar icon, and when a date is chosen show only the date (no X, no calendar icon).
  - _Done:_ Plan: center tasks-table headers + content, fix 'סטאטוס' spelling app-wide, add a search icon to the PersonPicker field, and simplify the date picker trigger. Done: TaskTable passes a headerCenter className to the non-name TableHeaderCells (justify-content+margin-auto) and TaskTableRow wraps deadline/status cell content in centered divs (assignee already centered). Replaced 'סטטוס' with 'סטאטוס' in all UI strings + the grouping bucket/label/comparison across TasksTab/PreviousTasksTab/EffectivenessTab (kept each file internally consistent) and client.js comments. PersonPicker search input now has a leading @vibe/icons Search icon (placeholder text removed, aria-label kept). DatePickerPopover renders just a calendar icon when empty and only the date when set, removing the clear-X and the always-on icon. No deviations.
- **2026-06-05** — UI refinements: collapse button moved onto the list/card divider and revealed only on hover of that line; topic drag handle shows only on hover; add-point affordance reduced to a + that expands into an input on hover/focus (removed the 'הוסף נקודה' label)
  - _Why:_ Cleaner, monday-like affordances that stay out of the way until needed
  - _Requested:_ The collapse button should sit on the dividing line between the topics column and the rest of the app, and appear only on hover of the line. The topics drag handle should appear only on hover. Remove the 'add point' text — just a +, and on hover show a text input.
  - _Done:_ Plan: move the collapse button onto the list/card divider (hover-only), make the topic drag handle hover-only, and reduce the add-point affordance to a + that reveals an input on hover. Done: App wraps the IconButton in a fixed full-height divider strip centered on the sidebar boundary (--sidebar-w var); the circular chevron button is opacity:0 until .dividerHandle:hover (pinned to the edge when collapsed). TopicsTab topic .grip set to opacity:0 with hover reveal. The add-point block became a .addPoint wrap showing a + (.addPointPlus) by default and revealing .addPointInput on :hover/:focus-within (pure CSS), keeping continuous Enter entry; removed the addingPoint state and the 'הוסף נקודה' label. No deviations.
- **2026-06-05** — UI polish: uniform-size status badge; monday-style circular collapse button (chevron, no label); add 'משימה' header to the tasks table and make task names non-bold; center assignee avatars
  - _Why:_ Visual consistency with monday's native UI
  - _Requested:_ Status badge frame uniform size regardless of text; collapse button like monday's (circular chevron, no label); group/task header styling — task name non-bold and a 'משימה' header above the name column; center avatars
  - _Done:_ Plan: uniform status-badge size; monday-style circular collapse button without label; add a 'משימה' header to the tasks table with non-bold task names; center assignee avatars. Done: StatusBadge given a fixed min-width centered frame (+matching 'בחר סטאטוס' placeholder); App collapse button switched to a bordered circular chevron (lucide ChevronLeft/Right, no text label); TaskTable name-column header set to 'משימה' and row name weight set to normal; assignee cell content centered. No deviations.
- **2026-06-04** — Redesign PersonPicker to match monday's native people-column picker (search, suggested people with avatars, selected chips) via @vibe/core Combobox + Chips `0239d21`
  - _Why:_ UX consistency with monday's native people picker
  - _Requested:_ תחקור על people picker, זה צריך להתנהג ולהראות כמו התיבה שיש בעמודת people
  - _Done:_ Planned to redesign PersonPicker to look and behave like monday's native people-column picker using @vibe/core. Replaced the checkbox dialog with a Combobox (search + suggested-people list with avatars) plus removable Chips for the selection and an AvatarGroup trigger, keeping the same {selected,onChange} API so all four call sites are unchanged. Deviation: it initially fetched only board subscribers, so account users who were not subscribers could not be found; switched to a new accountUsers() SDK call (logged via safeApi, limited to 500, no pagination yet). Note: monday's invite-by-email row is not available to embedded apps, so it was omitted.
- **2026-06-03** — Switch the entire app layout direction from RTL to LTR to match monday `d932392`
  - _Why:_ Align the app direction with monday's UI
  - _Requested:_ אני רוצה שכל האפליקציה צהיה משמאל לימין, תואם מאנדיי
  - _Done:_ Planned a hard LTR conversion of the whole app: flip the document direction and remove the explicit RTL overrides, then correct the few CSS rules that hardcoded RTL-assuming physical sides. Changed index.html to dir="ltr", flipped all 7 dir="rtl" JSX wrappers to ltr, switched text-align: right->left in DiscussionList/TasksTab/PreviousTasksTab, and flipped the physical borders (DiscussionList panel separator + item accent, TopicsTab section accent) to their LTR side. Direction-agnostic rules (text-align: start/center, flex-direction: column) were left untouched, and ErrorDetailsModal stays LTR by design. Build and all 13 tests pass.
