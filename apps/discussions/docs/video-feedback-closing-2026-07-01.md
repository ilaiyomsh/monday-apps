# פידבק וידאו — "הערות סוגרות" (עידו, 01.07.2026) — בקלוג משימות

> מקור: סרטון Loom "הערות סוגרות" מאת עידו פיוטרקובסקי (8 דק', 486ש').
> https://www.loom.com/share/f551fb6552a149bf9f4c5e7af49b0be6
> תומלל ונותח מול הקוד ב-2026-07-01. סבב המשך ל-`video-feedback-backlog.md` (29.06).
> סטטוסים: ✅ הושלם · 🟡 חלקי/לוודא · ⬜ פתוח · ⚠️ סותר החלטה קודמת — דורש אישור

---

## סיכום כללי
עידו פותח ("הולך ומשתבח, הכיוון מאוד טוב") וסוגר ("איך אתה מתקדם זה מטורף"). באמצע ~20 נקודות
שיפור, רובן UX קטנות. שתי נקודות סותרות החלטות קודמות מהבקלוג — מסומנות ⚠️.

## סדר עדיפויות מוצע
1. **קל + אימפקט גבוה:** #4 (כיוון הוספה), #5 (התייחסות ריקה), #6 (רוחב+צלמית), #11 (בעיכוב), #17 (תוויות הגדרות), #1+#2 (header).
2. **חוצה-מסך, שווה השקעה:** #20 (overlay on-top) — פותר גם #15/#21.
3. **דורש החלטת משתמש:** ⚠️ #12 (דדליין ב-My Tasks — סותר בקלוג #7), #16 (שמירת Group by).
4. **בינוני:** #7 (הוספה inline), #9 (resize), #13 (multi-select), #18 (מיפוי הגדרות), #19 (תבנית סיכום).
5. **לוודא אם כבר קיים:** #8, #14.

---

## א. כרטיס הדיון — Header

### #1 — סידור שורת התאריך והאנשים `[קל]` ✅
`00:06–00:22` — תאריך | הפרדה | שעה, ואז רווח לבן, ואז מנהל / מרכז / משתתפים **באותה שורה**.
כרגע תופסים שורות ומורידים את הטבלה. "יש מספיק מקום בנדל"ן" — לנצל רוחב במקום לגלוש לגובה.
📍 `src/components/DiscussionCard/` (header) + `DiscussionCard.module.css`. מורכבות: נמוכה (layout).
**יושם (01.07):** `.participants` → `flex-direction: row` שורה אחת (תאריך·שעה, מוביל/מרכז/משתתפים);
מקס' 3 אווטארים + עיגול "+N" (`max={3}`); לחיצה על קבוצה פותחת `Dialog` עם רשימה מלאה (תמונה+שם).
מובייל נשאר תפריט `Info` נפתח/נסגר.

### #2 — מרווח לפני אייקון המנהל `[טריוויאלי]` ✅
`07:59` — מרווח ~½ ס"מ בין הטקסט ל-avatar של המנהל. 📍 אותו אזור header.
**יושם (01.07):** `gap` ב-`.peopleGroup` הועלה 6px→8px.

---

## ב. נושאים — TopicsTab

### #3 — אינדיקציית "התייחסות/update" על נקודה `[קל-בינוני]` ⬜
`00:28–00:35` — כשיש תגובה/עדכון על נקודה, להציג סימן קטן (badge/מספר) שרואים שיש התייחסות.
📍 `src/components/TopicPointRow/` + `TopicsTab`. הנתון קיים (`responses` נטען ב-`useTopics`).

### #4 — מודול בחירת לייבל (סטטוס/עדיפות) ייפתח כלפי מעלה `[קל]` ✅
`00:40–01:02` — מודול בחירת הלייבל של עמודת **סטטוס** ועמודת **עדיפות** ייפתח **כלפי מעלה**
(popover מעל ה-trigger, לא מתחתיו). עידו רוצה ששניהם יתנהגו כך.
📍 ה-status/priority label picker בטבלת המשימות (`TaskTableRow` / picker popover).
**הערה:** ההנחיה המקורית שנרשמה כאן ("הוספת נושא/נקודה כלפי מעלה") הייתה פרשנות שגויה של הסרטון —
הכוונה היא לכיוון פתיחת ה-popover של בחירת הלייבל, לא לסדר ההוספה.
**יושם (01.07):** `preferred: 'top-start'` + ברירת מחדל `'top-start'` ב-`TaskTableRow` (סטטוס+עדיפות)
וב-`MyTasksRow` (`StatusEditCell` המשותף). `computeFloatingPosition` עדיין מתהפך למטה אם אין מקום למעלה.

### #5 — אל תוסיף "התייחסות" ריקה אוטומטית `[קל]` ✅ (02.07)
`02:03–02:09` — "עד שלא מוסיף התייחסות, אל תוסיף כרגע". להסיר שדה/שורת התייחסות שנוצר by default;
שיופיע רק כשמוסיפים בפועל. 📍 `TopicPointRow` / `TopicsTab`.

### #6 — רוחב טבלת הנושאים: חצי + צלמית במקום "לדיון" `[קל]` ✅ (02.07)
`02:09–02:23` — המרחק בין "לדיון" לתוכן ענק. לצמצם ל~חצי רוחב, ולהחליף טקסט "לדיון"/"האם להציג"
ב-icon (צלמית). 📍 `TopicsTab.module.css` + כותרת עמודת notForDiscussion.

### #7 — הוספה בלחיצה על השורה (בלי כפתור +) `[בינוני]` ⬜
`02:35–02:52` — כמו החוויה ב-Monday: לחיצה על אזור ריק בשורה פותחת שדה כתיבה, כותבים, Enter, נוסף.
לא חובה ללחוץ על ה-+. 📍 `TopicsTab` (add-row affordance).

### #8 — כותרת תמיד ראשונה (A), רוחב מינימלי `[לוודא]` 🟡
`02:54` — עמודת השם נעולה ראשונה, רוחב מינימלי. כבר קיים חלקית (frozen name column) — לוודא רוחב.

---

## ג. משימות — TasksTab / TaskTable

### #9 — הרחבת עמודות (resize), לא רק הזזה `[בינוני]` ⬜
`03:16–03:24` — בטבלת המשימות אפשר רק לגרור עמודה למיקום, אין הרחבת רוחב.
📍 `src/components/TaskTable/` + `ResizeHandle` + `useColumnWidths` (התשתית קיימת ב-My Tasks; להביא ל-TaskTable).

### #10 — לחיצה על כותרת הסיכום/קבוצה תפתח אותה `[קל]` ⬜
`01:48` — "שגם הלחיצה על סיכום מנכ"ל תפתח אותו". להרחיב בלחיצה על הכותרת, לא רק על החץ.
📍 header של הקבוצה.

---

## ד. אפקטיביות — EffectivenessTab

### #11 — "בעיכוב" במקום "תקוע" + הגדרה מחדש `[קל-בינוני]` ✅ (02.07)
`03:36–03:46` — KPI בשם "בעיכוב" = *משימה שעבר הדדליין שלה ואיננה בסטטוס "בוצע"*.
📍 `src/components/EffectivenessTab/EffectivenessTab.jsx:49,66` — כרגע מזהה delayed לפי תווית סטטוס = 'תקוע'
(`t.statusID === stuckId`). לשנות ללוגיקה מבוססת-דדליין: `deadline < today && statusID !== doneId`.
גם תווית ב-`:180`. מורכבות: נמוכה-בינונית (שינוי הגדרה, לא רק טקסט).
**יושם (02.07), בהרחבה שסוכמה מול עידו/אילי:** כרטיסי KPI דינמיים — סה"כ ואז כרטיס לכל סטטוס שיש בו
משימות (אחוז, מספר בריחוף, צבע הסטטוס), וכרטיס "בעיכוב" מחושב תמיד אחרון: דדליין אתמול ומטה (השוואת
תאריך בלבד) + סטטוס שאינו באוסף "בוצע" הנבחר בשדה הגדרות חדש (`preferences.delayedDoneStatusIds`,
ריק = תווית ה-is_done). צבע אדום כהה ייחודי `#7d1128` (לא בפלטת monday) + צלמית שעון. עמודת "בעיכוב"
נוספה גם לגרף הסטטוסים (לחיצה מסננת; חפיפה מכוונת עם עמודת הסטטוס). לוגיקה ב-`effectiveness.js` + בדיקות.

---

## ה. המשימות שלי — MyTasksView

### #12 — עריכת דדליין `[בינוני]` ⚠️ סותר בקלוג #7
`04:21–04:32` — "זו משימה שלי — תן לי לשנות דדליין. אל תיתן להכניס לדיון, אבל כן דדליין."
⚠️ סותר החלטת בקלוג #7 (30.06: "יישאר קריאה-בלבד לפי בקשת המשתמש"). דורש אישור.
📍 `src/components/MyTasksView/MyTasksRow.jsx:145–149` — מסומן במפורש read-only. צריך editor + write path.

### #13 — Checkbox לבחירה מרובה `[בינוני]` ⬜
`04:32` — "תן לי את הריבוע שהיה במקומות אחרים" — לסמן הרבה יחד ולערוך עדיפות/סטטוס בכמות.
📍 `src/components/MyTasksView/MyTasksTable.jsx:14` — מתועד "no selection affordances". לייבא דפוס selection-bar מ-`TasksTab`.

### #14 — עמודת "הערות" גם כאן `[לוודא]` 🟡
`05:19–05:42` — נוטה להוסיף עמודת הערות — "יותר מיידי מ-updates".
📍 חלקית קיים — `MyTasksRow.jsx:96` תומך `showNotes`/`onNotesChange` (עמודת `taskNotesID`). לוודא הצגה/מיפוי.

### #15 — update לא יקפוץ/ידרוס + מונה `[בינוני]` ⬜
`04:39–04:45` — אותו באג overlay (ראה #20), + אם יש update להציג מספר "1" ליד השורה.

### #16 — Group by — שמירה? `[החלטה]` ⬜
`04:53–04:57` — עידו שואל אם ה-Group by חד-פעמי או נשמר. כרגע חד-פעמי (client-side). להחליט על persistence.

---

## ו. הגדרות — SettingsModal

### #17 — לא ברור לאיזה לוח שייך כל סעיף `[קל]` ⬜
`06:06–06:26` — "people on this board" / סידור עמודות / ניהול תבניות — לוח דיונים או משימות?
צריך תווית ברורה לכל סעיף (דיונים / משימות). 📍 `src/components/SettingsModal/` (tabs/labels).

### #18 — מיפוי עמודות מבלבל / לא מעודכן `[בינוני]` ⬜
`06:46–07:41` —
- "סטטוס של דיון" מול "סוג דיון" — לא ברור ההבדל.
- "נושאים לדיון" נראה לא מעודכן.
- שדה סטטוס אחד "נראה מוזר".
- שדות מיותרים (למשל "אם נדונה") — איך מסירים מיפוי שלא צריך.
📍 `SettingsModal` + `src/utils/mondayApi/boards.config.js` (labels / `*_SETTINGS_FIELDS`).

---

## ז. סיכום — SummaryTab

### #19 — הכנסת תבנית / "מדף קיים" לסיכום `[בינוני]` ⬜
`07:48–07:53` — אפשרות להכניס תבנית מוכנה / להביא מדף קיים לסיכום הדיון.
📍 `src/components/SummaryTab/`.

---

## ח. חוצה-מסכים — Overlays (חשוב)

### #20 — פופאוברים חייבים לצוף on-top ולא לדחוף תוכן `[בינוני]` ⬜
`01:07–01:11`, `04:39` — חוזר פעמיים: כשפותחים עורך/update זה דורס/דוחף שורות במקום לצוף מעל,
"כמו בלוח רגיל". הנקודה הכי חוזרת — פותרת גם #15/#21.
📍 `src/utils/overlayPlacement.js` + `DatePickerPopover` + כל ה-inline editors.

### #21 — מיקום טולטיפ על הקצה `[נמוך]` ⬜
`03:56–03:59` — אלמנט שנפתח על קצה המסך — למקם קצת נמוך יותר. עידו: "לא כזה חשוב".
📍 `src/utils/overlayPlacement.js`.

---

## נקודות שאושרו כתקינות בסרטון (ללא פעולה)
- רשם דיון = מרכז (`01:19`) · שדה חובה = תאריך (`01:32`) · נושאים מתחילים פתוחים (`01:45`)
- העדפות אוטומטי מעולה (`06:30`) · יומן עובד טוב (`05:51`) · סטטוס יפה מאוד (`03:51`)
