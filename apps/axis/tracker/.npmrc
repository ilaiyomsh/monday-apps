# Ignore patch-package errors in production builds
# This is needed because @mondaycom/apps-cli tries to run patch-package
# but the patches are not needed for production builds
legacy-peer-deps=true

