# Changelog - tracker

*Auto-generated. Source: `~/.change-tracker/changes.db`*

## 2.2.0 — 2026-07-17

- Telemetry hardening: the api-latency health signal ships a coarse latency bucket
  (fast/ok/slow/very_slow) instead of raw ms, so the transport can dedup it — an unbucketed
  ms would burn the session ship-cap and starve real error records.
- Axiom logging v2 telemetry — call sites (logger primitives already v2 from Phase 1c).
- View-tracking: `useViewTracking(logger, 'calendar')` in `MondayCalendar.jsx` and `useViewTracking(logger, 'dashboard')` in `Dashboard.jsx` — one `view_open` usage event per view per session.
- Boot health: `logger.health('boot_ok', { ms })` fired alongside `initSummary` at "Calendar fully interactive", reusing the same elapsed.
- API-latency health at the `safeApi()` funnel: `logger.health('api_ok', { tag, ms })` on success and `logger.health('api_fail', { tag, ms, err_code })` on terminal failure (transport dedups; no double-log alongside apiResponse/apiError).
- Test infra: global logger mock (`setupTests.js`) now exposes `track()`/`health()` mirroring the real v2 wire shape; added a focused lock test for the new call sites (`mondayApi/__tests__/telemetry-callsites.test.js`).
- Telemetry stays inert until the Axiom sink is active; wire schema matches `@axis/app-core` (shared `encodeDims`).

## 2026-07

### ✨ New Features

- **2026-07-02** — Ship browser logs to Axiom: new hardened shared transport (axiomTransport.ts) in @axis/app-core + Tracker sink adapter (axiomSink.js); Tracker deploy doubles as the monday-CDN CSP canary
  - _Why:_ Client-only apps have zero production observability — errors invisible outside the user's browser; enables incident triage by account/user id
  - _Requested:_ Tracker: ship browser logs to Axiom via new shared hardened transport in @axis/app-core + sink adapter (execution of TRACKER-AXIOM-EXECUTION-PLAN.md Phases 1+2, Tracker is the monday-CDN CSP canary)

### 🔒 Security

- **2026-07-17** — Phase 1c — port the v2 Axiom telemetry primitives into tracker's local logger + sink: add track()/health() (usage/health), ship error.message only scrubbed as err_msg, reconcile the envelope kind to the shared domain discriminator, and close a stack-frame privacy leak. `e2a1a61`
  - _Why:_ tracker runs its OWN logger + Axiom sink (deliberately not on @axis/app-core — test-locked), so the v2 wire format had to be authored here too. Before this, tracker shipped its RENDERING kind (simple/api/init/...) into the same app-errors dataset where app-core ships the domain kind (error/usage/health) — schema drift the #239 review flagged. tracker's firstStackFrame also had the same @-line leak app-core 0.1 fixed.
  - _Requested:_ Phase 1c בתוכנית v2 — להביא את tracker לפורמט ה-wire של v2 (track/health, err_msg מסונן, kind דומייני) בלי לשבור את מאגר הטסטים הנעול.
  - _Done:_ Added track()/health() to tracker's logger.js (INFO records with domainKind usage/health + alwaysShip, message via the shared encodeDims imported from @axis/app-core so the wire format can't drift). In axiomSink.js: imported scrubMessage from @axis/app-core and now ship error.message ONLY scrubbed, as err_msg (D2); reconciled the envelope kind to the DOMAIN discriminator (error default, init/initSummary to health, explicit domainKind wins) instead of tracker's rendering enum — closing the cross-app schema drift the PR #239 review flagged; let shouldShip bypass the WARN/ERROR policy for alwaysShip records (duplicates still drop first); and anchored firstStackFrame's Firefox @-frame match (no whitespace before @) so an email in error.message can never be mistaken for a frame and leak into stack1. Bumped 2.1.0 to 2.1.1. Verified: tracker suite 976 pass, +5 new tests, and the 10 pre-existing unrelated failures (RTL snapshots, an env flag, integration timeouts) are unchanged — no new failures introduced into the locked suite.

## 2026-07

### 🧪 Tests

- **2026-07-17** — high-scale test round (joins the 2.2.0 candidate) — `aggregateAll` 5,000-event equivalence vs an independent reference (+anchors), granularity conservation, `consolidateBarData` cap, and un-chunked `fetchItemsStatus`/`fetchItemsLinkedIds` id-query characterization (FOLLOW-UPS F13); no runtime changes `04a96e6`

## 2026-06

### 🐛 Bug Fixes

- **2026-06-21** — Calendar auto-reloads (sometimes 2-3x in a row) after the app has already rendered. Root cause: preloadLazyModals background-prefetches 5 lazy modal chunks via lazyRetry-wrapped importers, and a transient chunk-load failure (network/CDN) calls window.location.reload() even for an unrequested background prefetch. Fix: (1) new prefetchLazy() in lazyRetry.js does a silent best-effort prefetch that never reloads; preloadLazyModals uses it. (2) global per-session auto-reload cap (MAX_AUTO_RELOADS) in lazyRetry so chunk reloads can't chain to 2-3. React.lazy still loads on demand; genuine failure surfaces via the ErrorBoundary chunk screen.
  - _Why:_ User-reported bug: board blanks out and re-fetches on its own on every entry (intermittent, machine/network-specific). Reproduced via new lazyRetry unit tests; ruled out account-data, browser extension, and stale cache (still repros in Incognito).
  - _Requested:_ משתמש דיווח שהאפליקציה עולה ואז הדף עושה ריפרש אוטומטי, לפעמים פעמיים-שלוש ברצף. לא קרה בכניסה דרך אותו משתמש במחשב אחר; קורה הרבה אבל לא 100%; קורה גם ב-Incognito.

### ✨ New Features

- **2026-06-10** — Clicking a personal day-off in the Day-off absence overlay opens the request in a new Day-off tab via deep-link ({dayOffAppUrl}?app[itemId]={dayOffItemId}). Personal days only (dayOffKind==='personal'); only when dayOffAppUrl is a valid http(s) URL; otherwise silent no-op. `4140b5c`
  - _Why:_ Let reporters jump straight from a personal absence shown in the Tracker calendar to its editable/viewable request inside Day-off, instead of the read-only dead-end click that exists today.
  - _Requested:_ feature: לחיצה על יום חופש אישי בשכבת ההיעדרויות של Day-off ב-Tracker תפתח את הבקשה בטאב חדש ב-Day-off, דרך deep-link בפורמט {dayOffAppUrl}?app[itemId]={dayOffItemId}. רק עבור ימים אישיים (dayOffKind==='personal'), ורק כש-dayOffAppUrl הוא http(s) תקין; אחרת התעלמות שקטה כמו היום.
  - _Done:_ The approved plan was to add a pure buildDayOffDeepLink helper plus tests, and wire it into the existing read-only isDayOff guard in MondayCalendar.handleEventClick so a personal day-off opens its Day-off request in a new tab. Implemented exactly that: a new src/utils/dayOffDeepLink.js validating http(s) and producing {baseUrl}?app[itemId]={itemId} (returning null on invalid input), 17 unit tests covering it, and a guarded window.open(..., '_blank', 'noopener,noreferrer') for dayOffKind==='personal' events — general days and the missing-baseUrl case keep the existing silent no-op. There were no deviations from the plan. ESLint is clean and 965 tests pass; the 2 failures in networkErrorOnCreate.test.jsx were confirmed pre-existing (fail identically on a stashed clean tree).
- **2026-06-09** — Wire Day-off absence overlay into the calendar (DAY-OFF-INTEGRATION W4.2): load useDayOffAbsences beside holidays on range change, merge into enrichedEvents, parallel isDayOff read-only guards (click/long-press/drag/resize/delete/update/context-menu/approval-selection) `5cfdd33`
  - _Why:_ Day-off integration plan W4 - tracker must display vacations-board absences as a read-only parallel overlay (D10)
  - _Requested:_ Execute ledger task W4.2 of DAY-OFF-INTEGRATION-EXECUTION.md: wire the Day-off overlay into MondayCalendar.jsx with parallel isDayOff guards, holidays layer untouched per D10, plus guard tests
  - _Done:_ Planned to wire the W4.1 useDayOffAbsences overlay into MondayCalendar.jsx exactly like the holiday overlay (per frozen decision D10) with parallel isDayOff guards next to every behavioral isHoliday guard. Loaded absences beside holidays on range change, on the initial window (react-big-calendar never fires onRangeChange on mount - a local decision so the overlay appears before the first navigation), and on dayOff settings changes; merged them into enrichedEvents as a separate parallel overlay gated by showAbsences, leaving the holiday merge expression untouched. Added read-only isDayOff guards in handleEventClick, handleEventLongPress, draggableAccessor/resizableAccessor, the all-day update/delete wrappers (defense-in-depth), useCalendarSelection.handleEventContextMenu, useApproval.approveAllPending, and useCelebration. Verified with 11 new tests (5-test integration suite via the DnD-HOC-stub harness incl. a positive modal-open control, plus unit suites for the context-menu and approve-all guards); full suite 917/919 in all 3 timezones - the 2 failures are the documented pre-existing networkErrorOnCreate ones from before W4. Defaults keep the overlay fully inert so all existing behavior is preserved.
- **2026-06-09** — W4.1: new useDayOffAbsences hook - read-only Day-off vacations-board absence overlay loader (two-query overlap fetch, multi-day all-day events, approval flags per D2) `ff36191`
  - _Why:_ Day-off integration plan W4 - tracker must display absences from the Day-off vacations board (DAY-OFF-INTEGRATION-PLAN.md W4.1)
  - _Requested:_ Execute ledger task W4.1 of the Day-off integration: new hook src/hooks/useDayOffAbsences.js in tracker - items_page overlap query on the vacations board (person assigned_to_me OR kind=general via two merged queries), map each item to one multi-day read-only calendar event with type label title/color and approval flags, gated behind showAbsences + configured Day-off board, with tests
  - _Done:_ Planned a read-only overlay loader hook for the Day-off vacations board per integration task W4.1, modeled on useIsraeliHolidays and the useMondayEvents item mapping. Added src/hooks/useDayOffAbsences.js: two items_page queries through the safeApi funnel (widened-window date rule [windowStart-366d..windowEnd] AND person any_of assigned_to_me / kind any_of generalLabelId) merged client-side with dedupe, a client-side inclusive overlap filter, and a mapping of each board item to ONE multi-day all-day read-only calendar event (dayoff_<itemId>, exclusive end = endDate+1d, title/color keyed by type label ID, isPending/isApproved flags per the D2 approval policy) - no per-day expansion, replace-within-window on every load. Deviation from the W4.5 session note (not from the frozen plan/contract): added the optional additive setting dayOffRejectedLabelIds (+MappingTab MultiSelect, he/en i18n) so legitimately-rejected requests are excluded silently and only approval labels matching none of the three configured sets trigger the loud aggregated mismatch error required by CONTRACT.md rule 3. The hook activates only when showAbsences is on and the full dayOff* mapping is present, so defaults keep it inert; verified with 22 new tests and the full timezone matrix (906/908 - the 2 failures are the pre-existing networkErrorOnCreate ones, confirmed on clean HEAD).
- **2026-06-09** — Day-off integration W4.5: settings foundation - absenceSource/showAbsences/dayOffApprovalRequired flags, dayOff* board/column/label mapping keys, manual mapping section in Settings dialog (D9), validator relaxed so allDayTypeStatusColumnId is required only when absenceSource is tracker `df6cde6`
  - _Why:_ Day-off integration plan W4 (tracker workstream) - settings keys must exist before W4.1-W4.4/W4.6 consume them
  - _Requested:_ Execute ledger task W4.5 of DAY-OFF-INTEGRATION-PLAN: add absence-source settings foundation + manual Day-off board mapping surface to tracker settings, relax allDayTypeStatusColumnId requirement; defaults must keep all ~855 tests green
  - _Done:_ Planned the W4.5 settings foundation for the Day-off integration: new settings keys with behavior-preserving defaults, a manual mapping surface per decision D9, and a relaxed validator. Added absenceSource ('tracker' default / 'dayoff'), showAbsences, dayOffApprovalRequired (D2), and the dayOff* vacations-board mapping keys (board, person, start/end dates, kind column + general/personal label IDs, type column, approval column + approved/pending label-ID sets) to DEFAULT_SETTINGS, plus a new 'absences' accordion in MappingTab with board/column pickers and stable label-ID selectors. Relaxed allDayTypeStatusColumnId to be required only when absenceSource is 'tracker' (settingsValidator.getRequiredSettings + the MappingTab required marker), and made the dayOff mapping required and loudly validated (boot validator with board/column existence checks on the vacations board, dialog-side validation, tab/accordion error routing) when absenceSource='dayoff'. Added 25 tests across settingsValidator, useSettingsValidation, and a new settingsErrorMeta suite; full suite + timezone matrix green except 2 pre-existing failures in networkErrorOnCreate.test.jsx that reproduce identically on HEAD (introduced before this change, likely by commit dcf17e0).
- **2026-06-02** — Unified UI error sink (Phase 1 of ui-sink-plan.md): logger becomes the single error-display path — new useUiErrorSink hook shows a toast for every ERROR record (with buffer replay + cap), showErrorWithDetails becomes a log-only facade, globalErrorHandler delegate removed, ErrorBoundary gains a retry button, all 121 logger.error sites aligned to pass Error objects, i18n-ready error message dictionary, test logger mock upgraded `3715513`
  - _Why:_ Close the gap between 121 logger.error sites and only 35 user-facing display calls — every caught error should surface to the user with full details; groundwork for a future external log sink
  - _Requested:_ מימוש UI Sink אחיד לשגיאות (Phase 1 של docs/ui-sink-plan.md): יישור 121 אתרי logger.error להעברת Error, מילון הודעות מוכן ל-i18n, hook חדש useUiErrorSink שמציג טוסט על כל רשומת ERROR (כולל replay מה-buffer עם תקרה), הפיכת showErrorWithDetails ל-facade לוג-בלבד, הסרת ה-delegate מ-globalErrorHandler, כפתור נסה-שוב ב-ErrorBoundary, ושדרוג מוק ה-logger בטסטים
  - _Done:_ Plan: Implement the approved ui-sink-plan.md Phase 1 in waves — align all 121 logger.error sites to pass real Error objects plus an i18n-ready message dictionary (wave 0), then a single combined commit adding the useUiErrorSink hook (one toast per ERROR record + buffer replay with cap), turning showErrorWithDetails into a log-only facade, removing the globalErrorHandler display delegate, and adding an ErrorBoundary retry button (waves 1+2+3) — with tests alongside each wave. Done: Delivered across 4 commits (call-site alignment; combined sink/replay/single-path; adversarial-review fixes; docs sync) plus 2 follow-ups from live-browser testing. 855 tests green, lint clean, deployed to the live Monday app. Deviations: An 8-agent adversarial review surfaced confirmed blockers fixed beyond plan — correlation-id inheritance in safeApi (true one-record-per-failure), a global toast surface rendered in all AppContent branches, and removal of duplicate bulk-summary toasts. Live testing added two unplanned scope items: mapping raw browser network errors to Hebrew, and a dedicated chunk-load refresh screen + friendly render-crash screen (replacing a toast storm). Also: wave-0 fan-out agents reported edits they hadnt actually applied, so those were re-done manually.
- **2026-06-01** — End-to-end error handling & monitoring rollout: make logger sink-ready (single emit() chokepoint, sink registry, ring buffer) with a log-once/correlationId contract; close catch/monitor gaps (3 critical, 12 high, medium/low tail); harden the root ErrorBoundary above the providers; instrument unscheduled dark sources (i18n/holiday/exceljs); add ESLint regression rules (no-console/no-empty/catch-must-log). Remote sink target deferred. `d795049`
  - _Why:_ Ensure every error is both caught AND monitored (flows through logger to one central chokepoint), per the error-handling audit.
  - _Requested:_ Implement error-handling-implementation-plan.md end to end (Phase -1..6) autonomously via the workflow engine (tools/error-handling-rollout-workflow.js) on branch feat/error-handling-rollout, with a tests+lint gate and per-phase commits. Remote sink deferred. Treated as one change for the whole initiative.
  - _Done:_ Implemented end-to-end error handling and monitoring on branch feat/error-handling-rollout, driven by an autonomous workflow engine with a per-phase tests+lint gate and per-phase commits. Made the logger sink-ready (single emit() chokepoint, addSink/removeSink, ring buffer, flush, and a log-once contract via correlationId/__loggedId), fixed a live ReferenceError that blanked the project list, closed the 3 critical + 12 high + medium/low gaps (false-success on failed writes, silent swallows, a stuck settings spinner), routed showErrorWithDetails and the global handler through the logger with de-duplication, hoisted the root ErrorBoundary above the providers, instrumented previously-dark sources (i18n/holiday/exceljs), and added ESLint regression rules (no-console/no-empty/catch-must-log). An independent adversarial review then found 3 real defects, fixed in Phase 7: non-Error payloads were not rendered to the console, log-once did not cover plain-object errors, and the date formatters silently wrote a blank date on an Invalid Date (now they throw and surface). Cleanup added behavior tests for network-failure handling and the date/duration/dashboard guards, fixed an optimistic-skeleton leak, stabilized a flaky test, and synced RELEVANT-SOURCES. The remote sink target (Sentry/POST /logs/CSP) was intentionally deferred. Result: 838 tests passing (up from 742), 0 lint errors, across ~14 commits; not yet merged.

### 🔧 Feature Changes

- **2026-06-11** — Replace the W4.4 redirect notice in the all-day modal (dayoff mode) with two buttons: multiple time reports on top, absence report below opening the Day-off deep-link in a new tab; disabled with explanatory text when dayOffAppUrl is unset `e9cd0d1`
  - _Why:_ Day-off cutover UX — absence entry moved to Day-off; the notice box is unclear, users need two direct actions
  - _Requested:_ כעת דיווח היעדרות יומי יהיה מתוך day off, ולכן בלחיצה על אזור כל היום צריך להפתח תיבה עם 2 אפשרויות: למעלה דיווחי שעות מרובים, למטה דיווח היעדרות כאשר לחיצה פותחת את day off
  - _Done:_ Plan: rework the dayoff-mode all-day menu in AllDayEventModal — bulk-reports button on top, a new absence-report button below replacing the W4.4 redirect notice. Done: the notice (title/text/anchor) was removed; the new button opens dayOffAppUrl via window.open('_blank','noopener,noreferrer'), and renders disabled with the inline note 'לא מוגדר קישור לאפליקציית היעדרות' when no valid http(s) URL is configured; i18n keys allDayModal.dayoffRedirect.* were replaced by dayoffButton/dayoffButtonNoUrl (he+en) and the dayoff modal suite was rewritten (7 tests incl. button order, window.open spy, javascript:-URL guard). No deviations from the approved plan.
- **2026-06-11** — Remove the hebcal-based automatic Israeli-holidays layer entirely (useIsraeliHolidays, holidayUtils, @hebcal/core, showHolidays setting, isHoliday guards, related tests) for all installs; holidays display only via Day-off general absences `e9cd0d1`
  - _Why:_ Duplicate holiday display (auto layer + employee/company absence on the same date); Day-off is now the single absence source
  - _Requested:_ חגים - אנחנו משאירים רק את מה שבא מ-day off. כל השאר צריך להסיר לגמרי. ההסרה גלובלית - כן
  - _Done:_ Plan: globally remove the automatic Israeli-holidays layer so holidays appear only as Day-off company-wide absences (user decision — applies also to installs still on absenceSource='tracker'). Done: deleted useIsraeliHolidays.js, holidayUtils.js, holidayConfig.js and the @hebcal/core dependency; stripped every isHoliday merge/guard (MondayCalendar, CustomEvent, useCalendarSelection, useApproval, useCelebration) while preserving the isDayOff guards verbatim; removed the showHolidays setting, its CalendarTab toggle, i18n keys and the gc-event-holiday CSS; updated 7 test files plus CLAUDE.md, ARCHITECTURE.md and the integration ledger. Deviation: vite.config.js also referenced @hebcal/core in manualChunks (build failed until removed) — not in the original file map. Verified 940/942 tests across 3 timezones (the 2 failures are the documented pre-existing networkErrorOnCreate ones; suite shrinks 949→942 with the holiday tests gone).
- **2026-06-10** — Exclude REJECTED Day-off items from the calendar overlay regardless of the approval policy (D2 amendment / DEV-2): the rejected-label check moves out of the approvalRequired branch in useDayOffAbsences, so a rejected request never renders even when the policy toggle is OFF `8eeff0a`
  - _Why:_ User decision 2026-06-10 during UI testing: rejection only marks the item; rejected requests stay visible inside Day-off but must never appear in Planner/tracker. The shipped OFF-policy behavior followed the original D2 literally and showed them
  - _Requested:_ בקשות דחויות אין להציג לא בפלאנר ולא בטראקר, אבל כן ב-day off
  - _Done:_ Planned per the user's D2 amendment (DEV-2): rejected Day-off requests must never appear in consumers regardless of the approval-policy toggle. Moved the rejected-label exclusion in useDayOffAbsences out of the approvalRequired branch so it applies always; updated the OFF-policy test that previously locked the include-rejected behavior and added a documented-degradation case (no rejected mapping = no exclusion). Suite 948/950 with only the two known pre-existing networkErrorOnCreate failures (F1).
- **2026-06-10** — SettingsWizard: create the all-day sub-type column conditionally - skip it when absences are external (absenceSource='dayoff') per DAY-OFF-INTEGRATION W4.6 `49c241a`
  - _Why:_ Day-off integration plan W4.6 (D3/D5): when absences move to the Day-off vacations board, newly wizard-built Time Logs boards no longer need the All-day Type column; MonthlyBattery deliberately untouched per frozen D3
  - _Requested:_ Execute ledger task W4.6: in SettingsWizard/useBoardBuilder.js make creation of the all-day sub-type column conditional - created only when absenceSource is 'tracker' or unset; skipped when 'dayoff'. No battery code changes per D3. Wizard behavior for existing flows identical by default; test the conditional path.
  - _Done:_ Planned to gate the SettingsWizard's creation of the All-day Type sub-type status column on the absence source per DAY-OFF-INTEGRATION W4.6, leaving MonthlyBattery untouched per frozen decision D3. Made useBoardBuilder skip the column in both the full-board and portfolio flows when absenceSource === 'dayoff' (created when 'tracker' or unset), emitting a progress-log line on skip and producing allDayTypeStatusColumnId: null, which the W4.5 validator accepts under 'dayoff'; SettingsWizard now feeds absenceSource from the existing stored settings into builder.build. Added 7 tests: a hook-level matrix (default/tracker/dayoff across both flows with an over-skip guard on sibling columns) and wizard wiring tests asserting the stored value reaches build. Verified with the full suite plus the 3-timezone matrix (941/943 in each; the only 2 failures are the documented pre-existing networkErrorOnCreate ones, confirmed failing on HEAD before the change).
- **2026-06-10** — Day-off integration W4.4: entry redirect per D5 - gate the all-day vacation-type menu behind absenceSource (notice + deep-link to Day-off when 'dayoff'; bulk-reports flow survives) and gate createSingleAllDayEvent/handleUpdateAllDayEvent behind absenceSource==='tracker' `48e464f`
  - _Why:_ Day-off integration plan W4.4 (frozen decision D5) - pre-approved via the execution protocol
  - _Requested:_ Execute ledger task W4.4 of DAY-OFF-INTEGRATION-PLAN.md in the tracker app: entry redirect per D5
  - _Done:_ Planned to implement the Day-off integration entry redirect (ledger W4.4, frozen decision D5): replace the all-day vacation-type menu with a redirect notice when absenceSource='dayoff' while keeping the modal and bulk-reports flow alive, and gate the legacy write paths. Replaced AllDayEventModal's type-button row with a bilingual notice plus an optional deep-link driven by a new additive dayOffAppUrl setting (http/https only, configured in the MappingTab absences accordion), skipped the type-label column fetch under 'dayoff', and added defense-in-depth warn-and-return gates to createSingleAllDayEvent and handleUpdateAllDayEvent; createMultipleReports stays fully functional. Added 11 tests (6 modal component, 5 hook gate) locking the redirect, the deep-link safety gate, the surviving bulk flow, and the byte-identical default 'tracker' behavior; full suite 934/936 in all 3 timezones (the 2 failures are the documented pre-existing networkErrorOnCreate ones).

### 🎨 Design

- **2026-06-11** — Exactly 4px gap between the bottom of the date circle and the top of the first all-day event bar; colored event bars start at the same vertical line across all day columns (aligned with the today column) `e9cd0d1`
  - _Why:_ Asymmetric spacing between weekday rows and the company row in the all-day area
  - _Requested:_ צריך רווח עליון מעל אירוע של 4 פיקסלים, מהעיגול הכחול שמסמן את היום הנוכחי. ולוודא שכל הפסים בכל הימים מתאימים לגובה הזה של היום הנוכחי
  - _Done:_ Plan: pin the header geometry so the date-circle bottom sits exactly on the all-day boundary, then add a uniform 4px gap above the event bars. Done: .rbc-header min-height 85→72px with a fixed 20px day-name line (4+20+48=72 — circle bottom lands on the row boundary in every column), a desktop-only padding-top:4px on .rbc-allday-cell .rbc-row-content shifting all event rows together so the colored bars align to the same line as the today column, and the dynamic JS height calc in MondayCalendar gained the matching 4px. No deviations.
- **2026-06-10** — W4.3: Day-off absence event visuals - distinct read-only class, type-label color, filled vs hollow (pending) rendering per D2 `1edefb7`
  - _Why:_ Day-off integration plan W4 (tracker) - approved absences render filled, pending render hollow to signal awaiting approval when the approval policy is ON
  - _Requested:_ Execute ledger task W4.3 of DAY-OFF-INTEGRATION: add an isDayOff branch in CustomEvent plus calendar CSS - distinct class in the spirit of gc-event-holiday, color from the type label; per D2 a pending absence renders unfilled (transparent bg, colored border + text) when dayOffApprovalRequired is ON, approved renders filled; when OFF everything renders filled. RTL-safe, month/week/day views. Component tests for filled vs hollow.
  - _Done:_ Planned an isDayOff visual branch in CustomEvent plus calendar CSS, completing partial work left by an interrupted session. Added a gc-event-dayoff class (holiday-spirit read-only styling: default cursor, no hover affordance, hidden dnd anchors) with the color taken from the absence type label, and a gc-event-dayoff-pending hollow state per decision D2: when the approval policy is ON a pending absence renders with transparent background, colored border and colored text instead of the generic 0.5 pending opacity, while approved (or policy OFF) renders filled. CSS is direction-neutral (RTL-safe) and applies in month/week/day views; the 3-class pending selector outranks the allday !important border/shadow rules. Added 6 component tests locking filled vs hollow and the regular-pending opacity boundary; full suite 923/925 in all 3 timezones (the 2 failures are the documented pre-existing networkErrorOnCreate ones).
- **2026-06-08** — Hide event notes text on calendar events `ad93e54`
  - _Why:_ UI quality improvement
  - _Requested:_ אני רוצה הערות לא יופיעו על גבי האירוע
  - _Done:_ Planned to remove note text from calendar event cards while keeping existing title/time/status indicators intact. I updated CustomEvent rendering to stop outputting notes so events remain compact and less noisy. I updated the CustomEvent tests to assert that notes are not displayed for timed events and kept all other expectations unchanged. No deviations were required from the approved scope.

### ♻️  Refactor

- **2026-06-10** — W4.8: test consolidation for the Day-off W4 surface - pin the legacy all-day suites explicitly to absenceSource='tracker', lock the DEFAULT_SETTINGS day-off keys (defaults preserve current behavior), and close the W4.2 coverage gap on useCelebration's isDayOff guard; full TZ matrix `58f8cef`
  - _Why:_ Day-off integration execution plan - ledger task W4.8 (tests-only consolidation, zero production code changes)
  - _Requested:_ Execute ledger task W4.8 in app tracker: gate existing all-day suites by absenceSource (default tracker - keep passing unchanged); add/complete the overlay suite for useDayOffAbsences + calendar guards + visuals if earlier tasks left gaps; full TZ matrix green. Ref: DAY-OFF-INTEGRATION W4.8
  - _Done:_ Planned a tests-only consolidation of the Day-off W4 surface per ledger task W4.8. Pinned the legacy all-day suites (createAllDayEvent.test.jsx, useAllDayEvents.payload/softError) explicitly to absenceSource='tracker' so they keep locking the legacy write path even if the stored or default source flips at cutover (behavior-neutral today; useEventModals consumes no settings, so it was left untouched as source-agnostic). Added SettingsContext.dayoffDefaults.test.jsx locking the W4.4/W4.5 DEFAULT_SETTINGS day-off keys (fresh-instance defaults, pre-W4.5 blob merge-on-load back-compat, stored-'dayoff' precedence) - nothing asserted these defaults before, although they are the entire legacy-green guarantee. Added useCelebration.dayoff.test.js closing the last untested W4.2 guard (isDayOff events never count toward timed-hour milestones, with a positive control). Verified 947/949 in all 3 timezones (full matrix); the only 2 failures are the documented pre-existing networkErrorOnCreate ones, re-confirmed failing on clean HEAD before this change.

## 2026-05

### 🐛 Bug Fixes

- **2026-05-26** — לא ניתן להמיר אירוע זמני לאירוע מלא לאחר שעברו יומיים
  - _Why:_ דיווח משתמש / לקוח

### 🔧 Feature Changes

- **2026-05-26** — פישוט הגדרות נעילת דיווח לשתי אפשרויות: ללא הגבלה, או X ימים מתאריך הדיווח `8dde906`
  - _Why:_ דרישה עסקית חדשה
