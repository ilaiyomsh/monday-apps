node_modules/
dist/
build/
.env
.env.*
!.env.example
.DS_Store
*.log
.vite/
coverage/
.dev-live/

# Agent-session git worktrees (never committed)
.claude/worktrees/

# mapps CLI artifacts (manifest export dirs, upload leftovers)
.dev/
temp.zip
# `mapps init -l` writes the access token to ./.mappsrc — never commit it
.mappsrc

# monday-ops CI run outputs (state.json/settings are uploaded as Actions artifacts)
sandbox-out/
__pycache__/

# local SecureStorage dev/test backend artifact (memory-backend snapshot; never source)
local-secure-storage.db.json

# twyst-your-status cleanup workflow: raw scanner output, regenerated by every scan.
# baseline.json, CLEANUP_PLAN.md, CLEANUP_REPORT.md and audit/ ARE tracked — they are the
# record of what was decided (scripts/cleanup/README.md).
apps/twyst-your-status/.cleanup/raw/
